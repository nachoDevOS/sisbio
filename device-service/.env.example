# Token compartido con Laravel. DEBE ser idéntico al DEVICE_SERVICE_TOKEN
# del .env de Laravel. Sin este valor el servicio rechaza todas las peticiones.
DEVICE_SERVICE_TOKEN=

# Segundos de espera al conectar con un equipo (opcional, por defecto 5).
DEVICE_SERVICE_TIMEOUT=5
