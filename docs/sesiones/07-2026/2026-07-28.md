# Sesión de trabajo — 2026-07-28

> Registro de trabajo realizado por **nachoDev**. Rama: `master`.
>
> Filtro por situación de contrato en el listado de funcionarios. Dos trabajos,
> en dos sistemas: (1) el filtro `?contrato=` y el campo `has_contract` en la API
> de Datos Personales de **sysadmin** (Mamoré), y (2) el select **Todos / Con
> contrato / Sin contrato** en el listado de funcionarios de **SISBIO**, que
> además muestra el cargo y la dirección administrativa cuando se filtra por
> «con contrato».
>
> Después vinieron dos trabajos más: (3) la **consolidación** de la API en un
> solo listado y 2 archivos, y (4) el resto de SISBIO pasando a usar el contrato
> que ahora viene con cada persona (fichas, combos, listados y reportes).
>
> Y para cerrar el día, dos sobre la ficha del funcionario: (5) las
> **marcaciones** dentro de la ficha de Mamoré, y (6) esa tabla pasando a
> cargarse **por AJAX**, compartida por las dos fichas (Mamoré y SIAT). Más tres
> de navegación: (7) el **menú lateral** marca en qué pantalla se está, (8) se
> puede plegar a una **franja de iconos** desde la barra superior, y (9) la barra
> superior estrena **menú de cuenta** con el cierre de sesión. Y para cerrar,
> (10) «Horarios» pasa a llamarse **«Turnos»** en la interfaz y (11) se suma el
> listado de **turnos asignados** a cada funcionario.
>
> Al final del día, dos más: (12) un **modal de eliminación único** para todo el
> sistema, que pide el motivo de la baja, con la auditoría (quién borró y por
> qué) resuelta **dentro del modelo** por el trait `RegistersUserEvents`, y (13)
> los **turnos asignados dentro de la ficha** del funcionario, con las
> asignaciones vencidas a un clic. Y (14) el alta: ahora se puede **asignar un
> turno** desde el listado o desde la propia ficha. Cerrando el día, (15) esas
> tres tablas de la ficha pasaron a **solapas**, cada una con su AJAX y su
> paginación, (16) el modal de **marcación manual** se volvió reutilizable:
> ahora también se anota desde la ficha del funcionario, (17) lo mismo con las
> **licencias**, en un modal acotado a esa persona y sus turnos vigentes, y (18)
> esas licencias también se **eliminan** desde la ficha. Cierra (19) el modal
> para **asignar turnos** desde la ficha, con el marcador que distingue cuál de
> los formularios rebotó, (20) el cierre del ciclo: **concluir** un turno con
> fecha de fin, y eliminarlo solo cuando se cargó mal, y (21) el **selector de
> turno**, que ahora muestra horario, jornada y tolerancia antes de elegir.
>
> El trabajo del lado de la API quedó documentado en detalle en la sesión de ese
> repositorio: `sysadmin/docs/nachodev/sesion/07-2026/2026-07-28.md` (5 trabajos:
> filtro por contrato, catálogos de direcciones y unidades, totales en el `meta`,
> buscador ampliado al puesto y la consolidación).

---

## Trabajo 1 — Filtro por contrato en la API de Mamoré (sysadmin)

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(api): filtrar personas por situación de contrato`

**Repositorio:** `d:\garoto\gadbeni\sysadmin`

### Problema
El listado de funcionarios de SISBIO necesitaba separar a los que tienen
contrato firmado de los que no. La API entregaba las dos cosas por separado y
sin forma de cruzarlas:

- `GET /people` → las 4.595 personas, sin saber cuáles son funcionarios.
- `GET /contracts` → los 1.040 contratos firmados.

«Sin contrato» era la resta de ambos conjuntos, y esa resta no se podía hacer
del lado de SISBIO sin bajar las dos listas completas: **57 peticiones** (46 de
personas + 11 de contratos) contra un límite de **60 por minuto por IP**. Con
paginación aparte: no hay manera de saber en qué página cae cada persona sin
tener el conjunto entero.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/Api/PersonalDataController.php` | Nuevo parámetro `?contrato=todos\|con\|sin`, resuelto con `whereHas` / `whereDoesntHave` sobre los contratos en estado `firmado`. Consulta base común (`baseQuery()`) con `withCount` de contratos firmados, para que `show` y `showByCi` también informen la situación. Totales `meta.total_con_contrato` / `total_sin_contrato`. Los enlaces de paginación conservan los filtros (`withQueryString()`). |
| `app/Http/Resources/PersonResource.php` | Nuevo campo `has_contract` (booleano), presente solo cuando la consulta trajo el conteo. |
| `app/Http/Controllers/Api/AdministrativeStructureController.php` | **Nuevo.** Catálogos `GET /directions` y `GET /units` (sin paginar), con `contratos_count` y filtros `search`, `activas`, `con_contratos`, `direccion`. |
| `app/Http/Resources/{Direccion,Unidad}Resource.php` | **Nuevos.** Campos de cada catálogo. |
| `app/Http/Controllers/Api/SignedContractsController.php` | El buscador ahora también pega en código de contrato, denominación, ítem y cargo de planilla. |
| `app/Models/{Direccion,Unidad}.php` | Relación `contracts()` para poder contar los funcionarios en funciones de cada una. |
| `routes/api.php` | 2 rutas `GET` nuevas (8 en total en el grupo `personal`). |
| `docs/nachodev/API-DATOS-PERSONALES.md` | Documentado el parámetro, el campo nuevo, los totales del `meta`, las secciones 5.4 y 5.5 (catálogos) y los ejemplos con `curl`. Corregidos los totales de ejemplo (decían 92, son 4.595). |

### Solución
El filtro es **del lado del servidor**, así que `meta.total` y la paginación
corresponden al subconjunto pedido: una sola petición por página, igual que
antes. Un valor desconocido en `?contrato=` cae en `todos`, para que un error de
tipeo nunca deje la tabla vacía.

Aparte del filtro, cada fila de `/people` ahora trae `has_contract`. Eso permite
marcar en la tabla quién es funcionario en funciones **sin una petición extra por
fila**, que era la otra opción.

### Verificación
Contra la base real, con el servidor de desarrollo levantado:

```bash
curl -H "X-API-KEY: ***" "http://127.0.0.1:8000/api/personal/people?limit=1&contrato=todos"
curl -H "X-API-KEY: ***" "http://127.0.0.1:8000/api/personal/people?limit=1&contrato=con"
curl -H "X-API-KEY: ***" "http://127.0.0.1:8000/api/personal/people?limit=1&contrato=sin"
```

| Filtro | Total | `has_contract` de la primera fila |
|---|---|---|
| `todos` | 4.595 | `true` |
| `con` | 1.040 | `true` |
| `sin` | 3.555 | `false` |

Los números cierran: **1.040 + 3.555 = 4.595**, y el total de «con contrato»
coincide exacto con el total de `/contracts`.

> **Nota:** no se agregaron pruebas automáticas en `sysadmin`. Ese proyecto no
> tiene base de datos de pruebas configurada (`phpunit.xml` tiene el SQLite en
> memoria comentado), así que una prueba con `RefreshDatabase` correría contra la
> base real y la borraría. La verificación fue contra datos reales, arriba.

---

## Trabajo 2 — Select «Todos / Con contrato / Sin contrato» en SISBIO

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(funcionarios): filtrar el listado por situación de contrato`

### Problema
El listado de funcionarios solo permitía elegir la fuente (Mamoré / SIAT) y
buscar por CI o nombre. No había forma de ver únicamente a los funcionarios en
funciones, ni de detectar a las personas registradas que no tienen contrato.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Services/MamoreClient.php` | `people()` acepta el filtro `$contrato` y lo manda como `?contrato=`. Nuevo `contracts()` para el listado de contratos firmados. Las tres peticiones comparten un `pedir()` privado que traduce fallos de red y estados de error a `MamoreException`. |
| `app/Services/DirectorioMamore.php` | Nuevos `normalizarContratos()` y `normalizarContrato()`: un contrato se normaliza a la **fila del funcionario** más su cargo y su dirección administrativa. Toda fila lleva ahora `cargo`, `direccion` y `conContrato`. |
| `app/Http/Controllers/PersonaController.php` | `list()` lee y valida el filtro `contrato`. Según el valor, la fuente es `/contracts` («con») o `/people` con el filtro («todos» y «sin»). Con la fuente SIAT el filtro se ignora. `totalesPorContrato()` traduce el `meta` de la API a los totales del select. |
| `resources/views/funcionarios/index.blade.php` | Select nuevo, que se oculta al cambiar a SIAT, con el total de cada situación en su etiqueta. |
| `resources/views/funcionarios/list.blade.php` | Columnas **Cargo** y **Dirección** cuando el filtro es «con contrato»; etiqueta verde/roja «Con contrato» / «Sin contrato» junto al nombre cuando el filtro es «todos»; totales para el select. |
| `tests/Feature/PersonaControllerTest.php` | Cinco pruebas nuevas: cada filtro pega en el endpoint correcto, SIAT no dispara peticiones, un valor inválido cae en «todos», y el parcial publica los totales. |

### Solución
Cada opción del select usa el endpoint que mejor responde:

- **Todos** y **Sin contrato** → `/people`, con el filtro nuevo del Trabajo 1.
- **Con contrato** → `/contracts`, que además del funcionario trae **gratis** el
  cargo y la dirección administrativa. Por eso la tabla suma esas dos columnas
  solo en esa opción.

En «con contrato» la fila muestra el **id de la persona**, no el del contrato, y
el botón «Ver» sigue apuntando a la ficha por cédula: la tabla no cambia de
identidad al cambiar de filtro.

La búsqueda por varias palabras (que la API no cruza) sigue funcionando igual en
las tres opciones: se trae un lote por el término más largo y se filtra
localmente por todas las palabras.

**Totales en el select.** Cada opción muestra cuántos hay: «Con contrato
(1.040)», «Sin contrato (3.555)». Los números salen del `meta` de la API, así que
siguen la búsqueda activa. Cuando el número **no** se puede saber con exactitud
—en «con contrato», donde la fuente es `/contracts` y no conoce a los que no
tienen; o con búsqueda de varias palabras, que se filtra localmente— la etiqueta
se muestra sin número, en vez de arrastrar un total viejo que ya no corresponde.

### Verificación
```bash
php artisan test --compact --filter="Persona|Licencia|Reporte|Marcacion"
# 136 pruebas, 136 pasadas, 450 aserciones
vendor/bin/pint --dirty --format agent
# passed
```

Y contra la API real, renderizando el parcial del listado con un usuario
autenticado (no con respuestas simuladas):

| Filtro | Filas | Totales del select | Columnas de contrato | Primera fila |
|---|---|---|---|---|
| Todos | 10 | todos=4595, con=1040, sin=3555 | no | PORFIDIO DACOSTA GUARAYUCO |
| Con contrato | 10 | con=1040 | **sí** (Cargo + Dirección) | LUIS CARLOS ALPIRE DURAN |
| Sin contrato | 10 | todos=4595, con=1040, sin=3555 | no | CLAUDIA VARGAS |

En «Todos» las 10 filas salieron con su etiqueta (6 con contrato, 4 sin). En
«Sin contrato», las 10 con etiqueta roja. En «Con contrato», el cargo y la
dirección de cada uno.

> ⚠️ **Este diseño de dos fuentes se reemplazó el mismo día** por el del Trabajo
> 3: hoy todo sale de `/people`. Lo de arriba queda como registro de cómo se
> llegó ahí.

---

## Trabajo 3 — Consolidación: un solo listado y menos archivos

**Commit:** `(pendiente de commit)` — mensaje sugerido: `refactor(api): un solo listado de personas con su contrato embebido`

### Problema
La API había crecido a **8 endpoints repartidos en 7 archivos** (3 controladores,
4 recursos) más 2 documentos de 1.346 líneas. Demasiada superficie para lo que en
realidad se necesita, y el consumidor tenía que elegir entre dos listados según
lo que quisiera ver.

Y el pedido de fondo era más simple de lo que el diseño reflejaba: **una sola
lista con todas las personas**, donde quien tiene contrato lo muestre con sus
datos y quien no lo tiene aparezca igual.

### Archivos modificados
**En Mamoré (`sysadmin`):**

| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/Api/PersonalApiController.php` | **Nuevo, único controlador.** `people`, `show`, `showByCi` y `catalogos`. Absorbe los tres controladores anteriores. |
| `app/Http/Resources/PersonResource.php` | **Único recurso.** La persona con su contrato firmado embebido (`contrato`) o `null`. Absorbe `ContractResource`, `DireccionResource` y `UnidadResource`. |
| `routes/api.php` | 4 rutas en vez de 8. |
| `docs/nachodev/API-DATOS-PERSONALES.md` | Reescrito: referencia + guía de conexión en un solo documento (688 líneas en vez de 1.346 en dos). |
| **Borrados** | `PersonalDataController`, `SignedContractsController`, `AdministrativeStructureController`, `ContractResource`, `DireccionResource`, `UnidadResource`, `API-CONEXION.md`. |

**En SISBIO:**

| Archivo | Cambio |
|---|---|
| `app/Services/MamoreClient.php` | Se fue `contracts()`: queda un solo listado. |
| `app/Services/DirectorioMamore.php` | Se fueron `normalizarContratos()` y `normalizarContrato()`. El cargo y la dirección salen del `contrato` que trae cada persona. |
| `app/Http/Controllers/PersonaController.php` | `funcionariosMamore()` sin ramas: una sola fuente. `totalesPorContrato()` sin el caso especial de «con». |
| `resources/views/funcionarios/list.blade.php` | Cargo y Dirección se muestran **siempre** (con `—` en quien no tiene contrato); la etiqueta Con/Sin queda solo en «Todos», donde aporta. |
| `tests/Feature/PersonaControllerTest.php` | La prueba de «con contrato» ahora simula `/people`; se agregó una que verifica que **con y sin contrato salen juntos en la misma tabla**, en **una sola petición**. |

### Solución
Los 8 endpoints quedaron en **4**:

```
GET /people          ?page= &limit= &search= &contrato= &direccion= &unidad=
GET /people/{id}
GET /people/ci/{ci}
GET /catalogos       ?activas= &con_contratos=
```

Cada persona de `/people` viaja así:

```json
{ "full_name": "LUIS CARLOS ALPIRE DURAN", "has_contract": true,
  "contrato": { "code": "SDAF-132/2026", "cargo_completo": "APOYO ADMINISTRATIVO - (Analista II)",
                "direccion_administrativa": { "sigla": "SDAF" }, "start": "2026-07-14" } }

{ "full_name": "CLAUDIA VARGAS", "has_contract": false, "contrato": null }
```

Qué se ganó, además de los archivos:

- **`/people/ci/{ci}` ahora funciona para todos.** Antes, para ver el contrato de
  alguien había que pegarle a `/contracts/ci/{ci}`, que devolvía 404 si la persona
  no tenía contrato. Ahora una sola consulta sirve para cualquiera.
- **Los totales del select ya están siempre.** Antes, en «con contrato» la fuente
  era `/contracts` y no conocía a los que no tienen: la etiqueta perdía el número.
  Ahora las tres opciones muestran «Con contrato (1.040)» / «Sin contrato (3.555)».
- **Buscar por cargo funciona en toda la lista.** `?search=Analista II` devuelve
  150 personas desde el mismo endpoint.
- **Filtrar por dirección y unidad** también quedó en `/people`
  (`?direccion=16&unidad=6` → 37 personas), sin cambiar de endpoint.

**Un detalle de Laravel 10 que condicionó el código.** El contrato se trae con un
eager load filtrado, sin `limit(1)`:

```php
'contracts' => fn ($query) => $query->where('status', 'firmado')
    ->with(self::RELACIONES_CONTRATO)
    ->orderByDesc('id'),
```

En Laravel 10 un `limit()` dentro de un `with()` se aplica a la **consulta
entera**, no por persona: con `limit(1)` solo la primera persona de la página
habría recibido su contrato. El límite por fila recién llegó en Laravel 11. El
recurso toma el primero del grupo, que por el orden es el contrato más reciente.

### Verificación
```bash
php artisan test --compact --filter="Persona|Licencia|Reporte|Marcacion"
# 137 pruebas, 137 pasadas, 458 aserciones
vendor/bin/pint --dirty --format agent
# passed
php artisan route:list --path=personal   # (en sysadmin) → 4 rutas
```

Contra la API real, renderizando el parcial con un usuario autenticado:

| Filtro | Filas | Totales del select | Columna Cargo | Etiquetas con/sin | Primera fila |
|---|---|---|---|---|---|
| Todos | 10 | con=1040, sin=3555 | sí | 6 / 4 | PORFIDIO DACOSTA GUARAYUCO |
| Con contrato | 10 | con=1040, sin=3555 | sí | — | PORFIDIO DACOSTA GUARAYUCO |
| Sin contrato | 10 | con=1040, sin=3555 | sí | — | CLAUDIA VARGAS |

Y la API directo:

```
people?contrato=todos → 4595 | 1ra: PORFIDIO DACOSTA GUARAYUCO  has=true  → DEGO-044/2026 | ASESOR DE DIRECCION - (Asesor Técnico I) | DEGO
                              | 3ra: JHAN CARLOS VARGAS         has=false → null
people?contrato=con   → 1040 | todas con contrato
people?contrato=sin   → 3555 | todas con contrato=null
people/ci/1938650            → CLAUDIA VARGAS | has=false | contrato=null   (antes: 404)
people/ci/000                → 404
catalogos?con_contratos=1    → 46 direcciones, 166 unidades
```

> Los archivos borrados quedaron respaldados en el scratchpad de la sesión antes
> de eliminarlos, y su contenido está absorbido en los dos archivos nuevos.

---

## Trabajo 4 — El resto del sistema pasa a usar la API

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat: usar el contrato de Mamoré en fichas, combos, listados y reportes`

### Problema
Con la API consolidada, el cargo y la dirección de cada funcionario vienen
**gratis** en cada consulta de persona. Pero el resto del sistema seguía como
antes: la ficha de Mamoré mostraba solo datos personales, los combos decían
«CI — NOMBRE» a secas, los listados de marcaciones y licencias solo el nombre, y
el **combo de reportes seguía leyendo de SIAT**, la base local, que no sabe nada
de contratos.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Services/DirectorioMamore.php` | Nuevo `etiqueta()`: el texto de los combos, «CI — NOMBRE · cargo (SIGLA)». Nuevo campo `nombreFormal` («Apellidos Nombres»), que es como imprime el reporte. |
| `app/Services/ResolutorNombres.php` | Pasa de resolver **nombres** a resolver **fichas** (nombre, cargo, dirección, PIN, origen). Nuevos `fichasPorCi()` y `fichaPorCi()`; `porCi()` queda como atajo. La caché de un día ahora guarda la ficha entera. |
| `resources/views/funcionarios/mamore-show.blade.php` | Tarjeta **«Contrato vigente»** (cargo, denominación, dirección, unidad, código, tipo de proceso, vigencia y lugar de trabajo), etiqueta Con/Sin contrato en el título, y aviso cuando la persona no tiene contrato. |
| `app/Http/Controllers/LicenciaController.php` | El combo y las fichas ya elegidas usan `etiqueta()`. El listado pasa fichas en vez de nombres. |
| `resources/views/licencias/create.blade.php` | La ficha del funcionario elegido muestra **Cargo** y **Dirección administrativa**. |
| `resources/views/licencias/list.blade.php` | Bajo el CI, el cargo del funcionario. |
| `app/Http/Controllers/MarcacionController.php` + `resources/views/marcaciones/list.blade.php` | La columna «Funcionario» agrega el cargo. |
| `app/Http/Controllers/ReporteMarcacionController.php` | El combo pasa a buscar **en Mamoré** (con SIAT como respaldo). La ficha del reporte la resuelve `ResolutorNombres`, y las marcaciones se cruzan por CI en vez de por la relación del modelo local. Se eliminó `ubicarFuncionario()`. |
| `resources/views/reportes/marcaciones/sinProcesar/{print,lista}.blade.php` | Consumen la ficha y muestran **Cargo** y **Dirección** (también en el QR del imprimible). |
| `app/Http/Controllers/PersonaController.php` | El reporte imprimible del funcionario local también arma su ficha con Mamoré primero. |
| `tests/Pest.php` | `fakeMamore()` acepta contratos: `ci => ['nombre' => …, 'cargo' => …, 'direccion' => …]`, y su `/people` respeta `?contrato=` y devuelve los totales, como la API real. |
| `tests/Feature/{Persona,Licencia,Marcacion,ReporteMarcacion}ControllerTest.php` | 8 pruebas nuevas. |

### Solución
El patrón quedó parejo en todo el sistema: **donde antes había un nombre, ahora
hay nombre + cargo**, y donde hay una ficha, además la dirección y la unidad.

`ResolutorNombres` era la pieza clave. Ya resolvía «CI → nombre» con Mamoré
primero y la base local de respaldo, cacheando un día. Ahora resuelve «CI →
ficha» con la misma estrategia, así que todas las pantallas que muestran gente a
partir de un CI suelto ganaron el cargo sin pedir nada extra: la caché guarda la
ficha completa, que ya venía en la misma respuesta.

**Dos decisiones que vale anotar:**

1. **El reporte imprimible conserva su formato.** Imprimía «Molina Guzman
   Ignacio» (apellidos primero), y `full_name` de Mamoré viene al revés. En vez
   de cambiar un documento que RRHH ya usa, se agregó `nombreFormal`, armado con
   los campos sueltos que la API expone (`paternal_surname`, `maternal_surname`,
   `first_name`, `middle_name`). El reporte sale igual que siempre, ahora con el
   cargo debajo.

2. **El combo de reportes cae a SIAT si Mamoré no responde.** Es el único punto
   donde el respaldo local se mantuvo a propósito: sin combo no se puede generar
   ningún reporte, y las marcaciones son locales de todas formas. En el listado
   de funcionarios, en cambio, SIAT sigue siendo una fuente que se elige a mano.

### Verificación
```bash
php artisan test --compact --filter="Persona|Licencia|Reporte|Marcacion"
# 145 pruebas, 145 pasadas
php artisan test --compact
# 265 pruebas, 259 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

Contra la API real, con un usuario autenticado:

```
ficha CON contrato: sección «Contrato vigente»=sí, cargo=sí, unidad DRRHH=sí, etiqueta=sí
ficha SIN contrato: aviso=sí, etiqueta «Sin contrato»=sí, sin sección de contrato
resolutor: LUIS CARLOS ALPIRE DURAN | formal=ALPIRE DURAN LUIS CARLOS
           | cargo=APOYO ADMINISTRATIVO - (Analista II) | dir=SDAF | origen=mamore
combo: 7604314 — LUIS CARLOS ALPIRE DURAN · APOYO ADMINISTRATIVO - (Analista II) (SDAF)
combo: 1712620 — MIGUEL VARGAS LEIGUE · Secretario Departamental de Administracion y Finanzas (SDAF)
combo: 1938650 — CLAUDIA VARGAS                (sin contrato: sin cargo, pero aparece igual)
```

---

## Trabajo 5 — Marcaciones en la ficha de Mamoré

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(funcionarios): mostrar las marcaciones en la ficha de Mamoré`

### Problema
La ficha de un funcionario de Mamoré (`/funcionarios/mamore/{ci}`) mostraba solo
datos personales, contacto y el contrato vigente. Las marcaciones estaban
únicamente en la ficha local (SIAT), así que para ver a qué hora marcó alguien
había que salir de la ficha y volver a buscarlo en otra pantalla.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/PersonaController.php` | `mamoreShow()` recibe el `Request`, arma el rango por defecto (del 1.º del mes a hoy) y busca la persona local por CI para saber si puede ofrecer el reporte imprimible. |
| `resources/views/funcionarios/mamore-show.blade.php` | Tarjeta **«Marcaciones»** con la tabla (fecha, hora, tipo con su etiqueta de color) y los filtros, igual que la ficha local. |
| `tests/Feature/PersonaControllerTest.php` | Pruebas nuevas: las marcaciones se cruzan por CI aunque la persona no exista en la base local, el filtro por rango y tipo, y el botón de reporte que solo aparece si la cédula está en la base local. |

### Solución
Las marcaciones viven en la tabla local `asistencias` y se relacionan **por
cédula**, no por el id de la persona. Eso permite listarlas para cualquier
funcionario de Mamoré sin exigir que exista en `personas`: alcanza con el CI que
ya trae la ficha.

El único punto que sí depende de la base local es el **reporte imprimible**: su
ruta (`funcionarios/{persona}/reporte-marcaciones`) resuelve un funcionario local
por CI, así que devolvería 404 para alguien que solo está en Mamoré. Por eso el
botón se muestra únicamente cuando esa cédula también está en `personas`.

> Al correr las pruebas apareció una que ya venía fallando: verificaba el código
> del contrato (`SDAF-132/2026`) en la ficha, pero ese campo había quedado
> comentado en la vista al ajustar el diseño. Se sacó esa aserción para que la
> prueba refleje lo que la pantalla muestra hoy.

---

## Trabajo 6 — La tabla de marcaciones de la ficha pasa a AJAX

**Commit:** `(pendiente de commit)` — mensaje sugerido: `refactor(funcionarios): cargar las marcaciones de la ficha por AJAX`

### Problema
La tabla del Trabajo 5 (y la de la ficha local, que era igual) se renderizaba con
la página: cada filtro y cada cambio de página recargaba la ficha entera —volvía
a consultar la API de Mamoré incluida— y las dos fichas tenían **la misma tabla
duplicada** en sus vistas. El resto del sistema (funcionarios, marcaciones,
licencias, horarios) ya usa el patrón *shell + parcial por AJAX*.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/PersonaController.php` | Nuevo `marcacionesList()`: filtra `asistencias` por CI, rango y tipo, y devuelve el parcial. `show()` y `mamoreShow()` ya no consultan marcaciones; solo pasan los valores iniciales de los filtros. |
| `routes/web.php` | Ruta nueva `funcionarios/ajax/marcaciones` (`funcionarios.marcaciones.list`). |
| `resources/views/funcionarios/marcaciones-list.blade.php` | **Nuevo.** El parcial: tabla + paginación. |
| `resources/views/funcionarios/marcaciones-panel.blade.php` | **Nuevo.** El panel compartido: filtros (Mostrar N registros, desde, hasta, tipo), contenedor de resultados y el script que carga el parcial. |
| `resources/views/funcionarios/{show,mamore-show}.blade.php` | Las dos fichas incluyen el mismo panel; se fueron las tablas duplicadas. |
| `tests/Feature/PersonaControllerTest.php` | Las pruebas de marcaciones pasaron a pegarle al endpoint AJAX; se sumaron las del panel en cada ficha, la del selector de registros por página y la de permisos sobre el endpoint. |

### Solución
Un solo endpoint sirve a las dos fichas, porque la consulta es por CI y no
depende de qué sistema muestre la ficha. El panel es un único archivo Blade que
ambas incluyen, con dos variables: la cédula a listar y la URL del reporte
(o `null` cuando esa cédula no está en la base local y el reporte no se ofrece).

Qué cambia para quien lo usa:

- Filtrar o cambiar de página **ya no recarga la ficha**: en la de Mamoré eso
  además evitaba una consulta a la API por cada clic.
- Se sumó el selector **«Mostrar N registros»** (10/25/50/100), como el resto de
  los listados. Antes eran 25 fijas.
- El botón **«Imprimir reporte»** se rearma en el momento con los filtros que
  están puestos, así el impreso siempre coincide con lo que se ve en pantalla.

### Verificación
```bash
php artisan test --compact tests/Feature/PersonaControllerTest.php
# 31 pruebas, 31 pasadas, 108 aserciones
php artisan test --compact
# 270 pruebas, 264 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 7 — El menú lateral marca en qué pantalla se está

**Commit:** `(pendiente de commit)` — mensaje sugerido: `fix(menu): marcar la opción activa y desplegar el grupo que la contiene`

### Problema
Al entrar a una pantalla no se sabía qué opción del menú estaba abierta. Dos
causas distintas:

1. **El grupo «Parámetros» solo se desplegaba en «Días excepcionales».** Contiene
   tres opciones (Días excepcionales, **Horarios** y **Licencias**), pero la
   condición de apertura miraba solo la primera. Estando en Horarios o en
   Licencias, el grupo aparecía plegado y la opción marcada quedaba **escondida
   adentro**: el menú no mostraba ninguna pista de dónde estaba parado el usuario.
2. **La marca era poco visible.** Solo cambiaba el fondo, un tono cercano al del
   propio menú.

Además, dentro del submenú, Horarios y Licencias usaban la clase de los enlaces
de primer nivel (`sidebar__link`) en vez de la de subopción, así que se veían con
otro tamaño y otra sangría que Días excepcionales.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/layouts/app.blade.php` | Las coincidencias de ruta se calculan una sola vez en un arreglo `$enMenu`, y `$enParametros` cubre **las tres** opciones del grupo. El botón del grupo se marca cuando la pantalla es una de las suyas. Los tres enlaces del submenú usan `sidebar__sublink`. Cada opción activa suma `aria-current="page"`. Script que trae la opción activa a la vista si el menú quedó con scroll. |
| `resources/views/layouts/app.blade.php` (estilos) | La marca activa suma **negrita** y una **barra verde a la izquierda** (`inset .2rem 0 0 var(--verde)`), en enlaces, subenlaces y en el botón del grupo. |
| `tests/Feature/BladeThemeTest.php` | Cuatro pruebas nuevas: la opción se marca, la marca se mantiene en las pantallas internas del módulo (alta/ficha/edición), el grupo se despliega y se marca en cualquiera de sus tres opciones, y queda plegado y sin marcar fuera de ellas. |

### Solución
La marca sigue saliendo de `request()->routeIs('modulo.*')`, así que **no se
pierde al entrar a una ficha o un formulario**: estando en la ficha de un
funcionario o editando un equipo, la opción del módulo sigue resaltada. Eso ya
funcionaba; lo que faltaba era el grupo.

El grupo «Parámetros» ahora arranca desplegado cuando la pantalla actual es
cualquiera de sus tres opciones, y su botón se marca igual que un enlace. Si el
usuario lo pliega a mano, la marca del botón sigue diciendo que la pantalla está
dentro de ese grupo.

`aria-current="page"` es el atributo estándar para «esta es la página actual»:
lo anuncian los lectores de pantalla y, de paso, es lo que usa el script para
encontrar la opción y traerla a la vista cuando el menú tiene scroll.

### Verificación
```bash
php artisan test --compact tests/Feature/BladeThemeTest.php
# 5 pruebas, 5 pasadas, 29 aserciones
php artisan test --compact
# 274 pruebas, 268 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# fixed (imports de la prueba nueva)
```

| Pantalla | Opción marcada | Grupo «Parámetros» |
|---|---|---|
| Equipos (listado, alta, ficha, edición) | Equipos | plegado y sin marcar |
| Horarios | Horarios (subopción) | **desplegado y marcado** |
| Licencias | Licencias (subopción) | **desplegado y marcado** |
| Días excepcionales | Días excepcionales (subopción) | **desplegado y marcado** |

---

## Trabajo 8 — Botón para plegar el menú lateral a una franja de iconos

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(menu): plegar el menú lateral desde la topbar en escritorio`

### Problema
El botón de las tres rayas de la barra superior estaba `display: none` y solo
aparecía **por debajo de 960px de ancho**. En una pantalla de escritorio no había
forma de ocultar el menú: los 15,5rem del lateral se comían el ancho útil en
listados anchos como el de funcionarios (ID, nombre, cargo, dirección, fecha de
nacimiento, PIN y acciones).

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/layouts/app.blade.php` | El botón pasa a estar **siempre visible** (con `:hover` propio). Nuevo estado `sidebarPlegado` en el `x-data` del `<body>`, con `alternarMenu()`: en móvil abre/cierra el menú sobre el contenido (como antes) y en escritorio lo pliega a la franja. La decisión de escritorio se guarda en `localStorage`. Un script mínimo al abrir el `<body>` aplica la clase antes de que arranque Alpine. Variable `--sidebar-rail: 3.5rem` y reglas `@media (min-width: 961px)` para la franja. El texto de cada opción pasó a un `<span class="sidebar__texto">` y cada una lleva `title`. |
| `tests/Feature/BladeThemeTest.php` | Tres pruebas nuevas: el botón está y es visible en escritorio, la franja (ancho, textos ocultos, `title`), y la preferencia guardada en el navegador. |

### Solución
Un solo botón con dos comportamientos, según el ancho de la ventana:

- **Móvil (≤960px):** el menú se desliza **encima** del contenido, como antes.
- **Escritorio (≥961px):** el menú se encoge a una **franja de 3,5rem con solo
  los iconos**, y el contenido gana el resto, con una transición de 0,2s.

La primera versión ocultaba el menú por completo (`translateX(-100%)`), pero así
no quedaba forma de navegar sin volver a desplegarlo. La franja conserva los
iconos, la marca de la opción activa (la barra verde) y el `title` de cada uno,
que nombra la opción al pasar el mouse.

Dos detalles que el cambio obligó a resolver:

1. **El texto de cada opción era un nodo suelto** dentro del enlace, así que no
   había forma de ocultarlo por CSS. Ahora va en su propio `span`.
2. **El submenú de «Parámetros» no cabe en la franja**: se oculta, y su botón
   pasa a desplegar el menú entero y abrir el grupo de un solo clic.

Como el sitio navega con recargas de página, la preferencia se guarda en
`localStorage`: quien pliega el menú lo mantiene plegado al cambiar de pantalla.
El script que aplica la clase va apenas se abre el `<body>` porque Alpine se
carga con `defer`: sin eso, en cada pantalla se vería el menú desplegado un
instante antes de plegarse.

---

## Trabajo 9 — Menú de la cuenta en la barra superior

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(topbar): menú de cuenta con cierre de sesión y baja del enlace a /admin`

### Problema
La barra superior tenía dos enlaces sueltos en texto plano: **«Volver al panel
→»**, que apuntaba a `/admin` —el panel de Filament, retirado el 20/07, o sea un
enlace muerto— y **«Salir»**, un botón sin icono ni confirmación visual, que no
se distinguía de un enlace cualquiera. Tampoco se veía **con qué usuario** se
estaba trabajando.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/layouts/app.blade.php` | Se fue el enlace a `/admin`. Nuevo menú de cuenta: botón con avatar (la foto de perfil si tiene, o sus iniciales), nombre y chevron; al abrirlo muestra nombre, correo y roles, y el botón **«Cerrar sesión»** en rojo con icono. Cierra con `Escape` o clic afuera. En pantallas chicas queda solo el avatar. |
| `tests/Feature/BladeThemeTest.php` | Dos pruebas nuevas: el menú muestra usuario, correo, rol, iniciales y el `POST` a `logout`; y ya no aparece el enlace al panel viejo. |

### Solución
El menú sale del usuario autenticado: `name`, `email`, sus roles y `avatar_path`
(la foto que ya se puede cargar desde Usuarios). Sin foto, el avatar son las
**iniciales** del nombre sobre el verde petróleo institucional.

«Cerrar sesión» sigue siendo un `POST` con CSRF a la misma ruta de antes: cambia
la presentación, no el mecanismo.

### Verificación
```bash
php artisan test --compact tests/Feature/BladeThemeTest.php
# 10 pruebas, 10 pasadas, 56 aserciones
php artisan test --compact
# 279 pruebas, 273 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 10 — «Horarios» pasa a llamarse «Turnos» en la interfaz

**Commit:** `(pendiente de commit)` — mensaje sugerido: `refactor(ui): hablar de turnos en vez de horarios en la pantalla de turnos`

### Problema
El módulo mezclaba dos nombres para lo mismo. El modelo es `Turno`, la tabla es
`turnos` y el campo es `nombreTurno`, pero la interfaz decía **«Horarios»** en
todos lados… salvo la columna de la tabla, que se llamaba «Horario» y mostraba
justamente el **nombre del turno**. Para quien usa el sistema, dos palabras
distintas para la misma cosa.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/layouts/app.blade.php` | La opción del menú pasa a **«Turnos»**. |
| `resources/views/horarios/index.blade.php` | Título «Administrador de turnos», botón «Nuevo turno», buscador «Buscar por nombre del turno…». |
| `resources/views/horarios/list.blade.php` | Columna **«Turno»**, confirmación de borrado y mensaje de tabla vacía. |
| `resources/views/horarios/{create,edit,show}.blade.php` | Títulos y botones: «Nuevo turno», «Guardar turno», «Editar turno», «Descripción del turno». |
| `resources/views/horarios/_form.blade.php` | «Descripción del turno» y «Nombre del turno». |
| `app/Http/Controllers/DiaTurnoController.php` | Los cuatro mensajes de confirmación/error hablan de turno. |
| `tests/Feature/DiaTurnoControllerTest.php` | La prueba del formulario de alta verifica el texto nuevo. |

### Solución
El cambio es **solo de interfaz**: rutas (`/horarios`, `horarios.index`…),
controlador, modelo, tabla y nombres de variables quedan igual. Tocar eso habría
sido un renombrado grande sin beneficio visible, y la URL vieja seguiría
apareciendo en los favoritos de la gente.

### Verificación
```bash
php artisan test --compact tests/Feature/DiaTurnoControllerTest.php
# 12 pruebas, 9 pasadas, 3 falladas (las mismas del 26/07, ver más abajo)
php artisan test --compact
# 279 pruebas, 273 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 11 — Listado de turnos asignados a los funcionarios

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(turnos): listado de turnos asignados por funcionario`

### Problema
La tabla `asignacion_turnos` (migrada de «AsignacionTurnos» del SIA) guarda qué
turno tiene cada funcionario y en qué rango de fechas, pero **no tenía ninguna
pantalla**: se podían ver los turnos y los funcionarios por separado, nunca la
relación entre los dos. La única forma de saber qué turno tiene alguien era
consultar la base a mano.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Models/AsignacionTurno.php` | Scope `buscar()` (CI, nombre del funcionario o nombre del turno), scope `vigenteEn()` y accesor `situacion` (vigente / vencida / futura). Comentado que la relación va por `turno_id`. |
| `app/Policies/AsignacionTurnoPolicy.php` | **Nueva.** Reutiliza los permisos de turnos (`ViewAny:DiaTurno`), así los roles no cambian. |
| `app/Http/Controllers/AsignacionTurnoController.php` | **Nuevo.** `index()` (shell con filtros) y `list()` (parcial por AJAX) con búsqueda, día del turno, situación y paginación. |
| `resources/views/turnos-asignados/{index,list}.blade.php` | **Nuevas.** Mismo patrón shell + parcial AJAX del resto de los listados. |
| `routes/web.php` | Dos rutas nuevas: `turnos-asignados` y su `ajax/list`. |
| `resources/views/layouts/app.blade.php` | Opción **«Turnos asignados»** en el grupo Parámetros, junto a Turnos. |
| `tests/Feature/AsignacionTurnoControllerTest.php` | **Nuevo.** 10 pruebas. |

### Solución
Las dos relaciones que pidió el pedido, explícitas:

- **Funcionario → por CI.** La asignación solo guarda la cédula. El nombre y el
  cargo los resuelve `ResolutorNombres` (Mamoré primero, base local de respaldo),
  igual que en marcaciones y licencias, y se consultan solo los CI distintos de
  la página.
- **Turno → por `turno_id`**, la clave foránea real contra `turnos.id`.
  **`idTurno` no se usa para vincular**: es el código de 3 caracteres que traía
  el SIA y queda solo como dato histórico.

Como en la copia del SIA `turno_id` puede haber quedado en `null` (cuando el
código histórico no cruzó con ningún turno migrado), esas filas no se esconden:
salen con una etiqueta **«Sin turno vinculado»**, que es justamente lo que hay
que corregir.

La tabla muestra funcionario (nombre, CI y cargo), turno, día, hora de entrada y
salida, la vigencia (desde–hasta) y una etiqueta de situación respecto de hoy:
**Vigente**, **Vencida** o **Aún no vigente**. Se puede filtrar por día del
turno, por situación y buscar por CI, funcionario o nombre del turno.

### Verificación
```bash
php artisan test --compact tests/Feature/AsignacionTurnoControllerTest.php
# 10 pruebas, 10 pasadas, 42 aserciones
php artisan test --compact
# 289 pruebas, 283 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 12 — Modal único de eliminación y auditoría de la baja en el modelo

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(bajas): modal único de eliminación con motivo y auditoría en el modelo`

### Problema
Cada listado confirmaba su borrado con el `confirm()` del navegador
(`onsubmit="return confirm('¿Eliminar…')"`): un cartel gris del sistema
operativo, sin contexto de qué se borra ni forma de anotar **por qué**. Equipos
era la excepción —tenía su propio modal con motivo obligatorio—, así que en el
sistema convivían dos formas distintas de dar de baja.

Y la auditoría estaba a medias: `deleteUser_id` y `deleteObservacion` existían en
casi todas las tablas, pero solo se llenaban donde alguien se acordaba de
mandarlas.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/layouts/app.blade.php` | **Modal global de eliminación**: un único `<form>` con `@csrf` + `@method('DELETE')` y `:action` dinámico, con textarea de motivo (obligatorio, 5–500), casilla de confirmación y estado en un **store de Alpine** (`Alpine.store('eliminar')`). Estilos propios (`modal-caja--peligro`, `modal-peligro__*`). El toaster muestra el rechazo del motivo. |
| `resources/views/components/boton-eliminar.blade.php` | **Nuevo.** Botón que no envía nada: carga en el modal la URL del `destroy` y el texto de lo que se borra. Variantes `icono` (fila) y `menu` (dentro de un `.dropdown-menu__peligro`). |
| `resources/views/{horarios,licencias,dias-excepcionales}/list.blade.php`, `{roles,usuarios,equipos}/index.blade.php` | Los seis puntos de baja usan el componente. Equipos perdió su modal propio (30 líneas duplicadas). |
| `app/Traits/RegistersUserEvents.php` | El hook `deleting` graba `deleteUser_id` y `deleteObservacion` (leído del request) y guarda, igual que `creating` ya hacía con `registerUser_id`. |
| `app/Models/{User,Role}.php` | Suman el trait: antes eran los únicos modelos del sistema sin auditoría. |
| `database/migrations/2026_07_28_094907_*` y `2026_07_28_140239_*` | Columnas de auditoría en `users` y `roles` (`registerUser_id`, `deleteUser_id`, `deleteObservacion`), que venían del esqueleto de Laravel y de spatie sin ellas. |
| `app/Http/Controllers/EquipoController.php` | `destroy()` valida el motivo (lo copia a la bitácora) y hace el borrado y la anotación **en una transacción**. |
| `app/Http/Controllers/LicenciaController.php` + `app/Models/AsignacionTurno.php` | El helper privado `turnosAsignados()` se mudó al scope `delFuncionario()` del modelo (ver Trabajo 13). |
| `tests/Feature/{ModalEliminarGlobal,RegistersUserEvents}Test.php` | **Nuevos.** 11 pruebas: el modal y su formulario único, el mínimo del motivo, el aviso por toaster, que los listados abren el modal en vez del `confirm()`, y los cuatro caminos del trait. |
| `tests/Feature/{User,Role,DiaTurno,DiaExcepcional,Licencia,Equipo}ControllerTest.php` | Las pruebas de baja mandan el motivo y verifican `deleteUser_id` / `deleteObservacion` en la fila. |

### Solución
Un modal, un formulario. El botón de cada fila solo dice **qué** se borra y
**adónde** mandar el `DELETE`:

```blade
<x-boton-eliminar :accion="route('roles.destroy', $rol)"
                  :mensaje="'Se elimina el rol «'.$rol->name.'».'" />
```

El estado vive en un **store** de Alpine y no en un `x-data` a propósito: las
tablas de turnos, licencias y días excepcionales se inyectan por AJAX, y un
botón recién insertado igual encuentra el store, sin importar dónde quede en el
árbol.

**La auditoría se resolvió dentro del modelo**, siguiendo el patrón que ya usaba
el sistema de kaiteki: el controlador solo hace `$modelo->delete()` y el trait
se encarga del resto.

```php
static::deleting(function (Model $model): void {
    if (Auth::check()) {
        $model->deleteUser_id = Auth::id();
        $model->deleteObservacion = request()->input('deleteObservacion');

        $model->save();
    }
});
```

Los seis `destroy()` del sistema quedaron en tres líneas: `authorize`, `delete`,
redirección. Ninguno menciona las columnas de auditoría.

> **Alcance del motivo obligatorio.** Lo exige el modal (`required` + mínimo de 5
> caracteres) y, del lado del servidor, solo **Equipos**, que además lo copia a
> su bitácora. Un `DELETE` armado por fuera del modal en los otros módulos borra
> con `deleteObservacion` en `null`. Se probó una versión donde el propio trait
> validaba y cortaba la baja, pero se descartó para que el trait quedara del
> tamaño del original: dos hooks y nada más.

### Verificación
```bash
php artisan test --compact tests/Feature/ModalEliminarGlobalTest.php tests/Feature/RegistersUserEventsTest.php
# 11 pruebas, 11 pasadas, 59 aserciones
php artisan test --compact
# 300 pruebas, 294 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 13 — Turnos asignados dentro de la ficha del funcionario

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(funcionarios): mostrar los turnos asignados en la ficha`

### Problema
El listado del Trabajo 11 responde «¿quién tiene este turno?», pero la pregunta
del día a día es la inversa: **«¿qué turno tiene esta persona?»**. Para saberlo
había que salir de la ficha, ir a Turnos asignados y buscar la cédula a mano. Y
las asignaciones **vencidas** no se podían revisar sin cambiar el filtro de esa
otra pantalla.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Models/AsignacionTurno.php` | Nuevo scope `delFuncionario($ci, $incluirVencidas)`: las asignaciones de una cédula, las que siguen en pie primero y después por día de la semana y hora de entrada. Sale del helper privado que tenía `LicenciaController`, que ahora lo reusa. |
| `app/Http/Controllers/PersonaController.php` | `show()` y `mamoreShow()` cargan las asignaciones de la cédula (vencidas incluidas). El helper devuelve vacío si el usuario no puede ver turnos asignados. |
| `resources/views/funcionarios/turnos-panel.blade.php` | **Nuevo.** El panel compartido: turno, día, entrada, salida, vigencia y etiqueta de situación. |
| `resources/views/funcionarios/{show,mamore-show}.blade.php` | Las dos fichas incluyen el panel, bajo `@can('viewAny', AsignacionTurno::class)`. |
| `tests/Feature/PersonaControllerTest.php` | Cinco pruebas: la ficha lista los turnos, separa los vencidos, avisa cuando no hay ninguno, la ficha de Mamoré también los muestra, y sin permiso el panel no se dibuja. |

### Solución
Las asignaciones llegan **todas en una sola consulta**, vencidas incluidas, pero
las vencidas arrancan **ocultas**: el pie del panel tiene un «Ver también las
vencidas (N)» que las muestra sin pedir nada al servidor. Así la ficha de alguien
con años de historial no arranca con una tabla larga de turnos que ya no rigen,
pero el dato está a un clic.

Cuando **todas** las asignaciones están vencidas, la tabla no queda vacía y muda:
dice «El funcionario no tiene turnos vigentes: los N que tuvo ya vencieron».

El panel sirve a las dos fichas —la local (SIAT) y la de Mamoré— porque, igual
que las marcaciones, la asignación se cruza **por cédula**: no hace falta que la
persona exista en la base local. Y encabeza con un atajo al listado global ya
filtrado por esa cédula.

El permiso es el mismo del listado (`ViewAny:DiaTurno`, vía la policy de
asignaciones): quien no puede ver turnos asignados no ve el panel, y el
controlador ni siquiera hace la consulta.

### Verificación
```bash
php artisan test --compact tests/Feature/PersonaControllerTest.php
# 36 pruebas, 36 pasadas
php artisan test --compact
# 305 pruebas, 299 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 14 — Asignar un turno nuevo, también desde la ficha

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(turnos): asignar un turno a un funcionario desde el listado o su ficha`

### Problema
El listado de turnos asignados y el panel de la ficha eran de **solo lectura**:
se veía qué turno tiene cada funcionario, pero para asignarle uno nuevo había
que tocar la base a mano. Y en la ficha del funcionario, que es donde uno se da
cuenta de que le falta el turno, no había ninguna acción para hacerlo.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Requests/StoreAsignacionTurnoRequest.php` | **Nuevo.** Valida funcionario, turno (que exista y no esté eliminado), rango de fechas (`hasta` ≥ `desde`) y observación. Autoriza en el propio request, y comprueba la asignación repetida contra la clave única de la tabla. |
| `app/Http/Controllers/AsignacionTurnoController.php` | `create()` (formulario, con `?ci=` ya elegido), `store()` y `buscarFuncionarios()` (combo contra Mamoré). `destino()` decide a dónde volver al guardar. |
| `app/Policies/AsignacionTurnoPolicy.php` | Nueva habilidad `create`, sobre el permiso `Create:DiaTurno`. |
| `resources/views/turnos-asignados/create.blade.php` | **Nueva.** Combo de funcionario, select de turnos agrupados por día, rango y observación. |
| `resources/views/turnos-asignados/index.blade.php` | Botón **«Asignar turno»** en la cabecera. |
| `resources/views/funcionarios/turnos-panel.blade.php` | Botón «Asignar turno» en la ficha, con el funcionario ya elegido. |
| `resources/views/funcionarios/{show,mamore-show}.blade.php` | Pasan el origen (`local` / `mamore`) al panel. |
| `routes/web.php` | Tres rutas nuevas: formulario, alta y búsqueda JSON de funcionarios. |
| `tests/Feature/AsignacionTurnoControllerTest.php` | 9 pruebas nuevas (19 en total en el archivo). |

### Solución
Desde la ficha el funcionario ya viene elegido, así que asignar un turno son
tres datos: **turno, desde y hasta**. Los turnos del select van agrupados por día
de la semana y muestran su horario, para no tener que memorizar los nombres.

Al guardar se vuelve **a donde se estaba**: si se entró desde una ficha, a esa
ficha; si se entró por el listado, al listado ya filtrado por ese funcionario.
El origen se limita a dos valores conocidos (`local` y `mamore`) y la URL se arma
del lado del servidor, así un valor manipulado nunca redirige fuera del sitio.

Tres detalles del guardado:

1. **La relación se crea por `turno_id`.** `idTurno` se copia del turno elegido
   —nunca llega del formulario— porque es parte de la clave única heredada del
   SIA, pero no es lo que vincula.
2. **La asignación repetida se avisa, no revienta.** La clave única
   (ci + idTurno + desde) **incluye las filas borradas lógicamente**, así que la
   comprobación usa `withTrashed()`: si no, un alta repetida terminaría en un
   error de base de datos en vez de un mensaje.
3. **La autorización va en el request**, no solo en el controlador: la
   validación corre antes que el `authorize()` del controlador, así que un
   usuario sin permiso habría recibido errores de campos en lugar del 403.

### Verificación
```bash
php artisan test --compact tests/Feature/AsignacionTurnoControllerTest.php
# 19 pruebas, 19 pasadas, 75 aserciones
php artisan test --compact
# 314 pruebas, 308 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 15 — Solapas en la ficha del funcionario

**Commit:** `(pendiente de commit)` — mensaje sugerido: `refactor(funcionarios): marcaciones, licencias y turnos en solapas con su propio AJAX`

### Problema
La ficha había acumulado **tres tablas apiladas** —turnos asignados,
marcaciones y licencias—, una debajo de la otra: para llegar a las licencias
había que bajar toda la pantalla. Además cada una funcionaba distinto: las
marcaciones por AJAX, los turnos con una colección completa en el HTML (sin
paginar, con las vencidas escondidas por CSS) y las licencias paginadas del lado
del servidor, que recargaban la ficha entera —y en la de Mamoré, con ella la
consulta a la API— cada vez que se cambiaba de página.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/PersonaController.php` | Nuevos `licenciasList()` y `turnosList()` (parciales por AJAX, filtrados por CI y paginados). `show()` y `mamoreShow()` ya no consultan licencias ni turnos; se eliminó el helper `turnosAsignados()`. |
| `routes/web.php` | Dos rutas nuevas: `funcionarios/ajax/licencias` y `funcionarios/ajax/turnos`. |
| `resources/views/funcionarios/paneles.blade.php` | **Nuevo.** Las tres solapas con sus filtros, sus contenedores y un único script que las maneja. |
| `resources/views/funcionarios/{licencias,turnos}-list.blade.php` | **Nuevos.** Los parciales de cada tabla, con su paginación. |
| `resources/views/funcionarios/{show,mamore-show}.blade.php` | Incluyen el panel de solapas en vez de las tres tarjetas. |
| `resources/views/layouts/app.blade.php` | Estilos `.tabs` / `.tabs__boton`. |
| **Borrados** | `marcaciones-panel.blade.php` y `turnos-panel.blade.php`, absorbidos por el panel de solapas. |
| `tests/Feature/{Persona,Licencia}ControllerTest.php` | Las pruebas que leían las tablas en el HTML de la ficha pasan a pegarle a su endpoint; se sumaron las de las solapas y los filtros de turnos. |

### Solución
Las tres tablas quedaron en un solo bloque con solapas: **Marcaciones**,
**Licencias** y **Turnos**. Cada una conserva sus filtros, su selector de
registros por página y su paginación, y todas cargan por AJAX contra su propio
endpoint, como el resto del sistema.

Dos decisiones que vale anotar:

1. **Cada solapa pide su tabla la primera vez que se la abre.** Entrar a la ficha
   dispara una sola consulta (la de marcaciones, que es la solapa inicial), no
   tres. Después la tabla queda cargada y cambiar de solapa es instantáneo.
2. **Las asignaciones vencidas ahora son un filtro, no un `x-show`.** Antes
   viajaban todas en el HTML y las vencidas se ocultaban por CSS: en alguien con
   años de historia eso es una tabla larguísima escondida. Ahora el filtro
   —Todas / Solo vigentes / Solo vencidas— se resuelve en la consulta, y el
   orden sigue poniendo primero las que están en pie.

Las solapas de Licencias y Turnos solo aparecen si el usuario tiene permiso para
verlas; el script se arma en consecuencia, así no pide un endpoint que le va a
responder 403.

### Verificación
```bash
php artisan test --compact tests/Feature/PersonaControllerTest.php
# 39 pruebas, 39 pasadas, 148 aserciones
php artisan test --compact
# 317 pruebas, 311 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 16 — Modal de marcación manual reutilizable

**Commit:** `(pendiente de commit)` — mensaje sugerido: `refactor(marcaciones): modal de alta manual como componente reutilizable`

### Problema
El modal «Nueva marcación manual» estaba escrito **dentro** de la pantalla de
Marcaciones: 30 líneas de formulario mezcladas con el encabezado. Desde la ficha
del funcionario —donde uno detecta que falta una marcación— no había forma de
anotarla: había que ir a Marcaciones y escribir el CI a mano.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/components/modal-marcacion.blade.php` | **Nuevo.** Botón + modal en un componente, con `ci` opcional (si viene, el funcionario ya está dado y el campo no se muestra) y `origen`. Chequea el permiso adentro y sufija los ids para que dos modales convivan en una pantalla. |
| `resources/views/marcaciones/index.blade.php` | Usa `<x-modal-marcacion />`; se fueron las ~30 líneas del formulario. |
| `resources/views/funcionarios/paneles.blade.php` | Botón «Nueva marcación» en la solapa de marcaciones, con el CI ya cargado. |
| `resources/views/funcionarios/{show,mamore-show}.blade.php` | Pasan `hayPersonaLocal` al panel. |
| `app/Http/Controllers/MarcacionController.php` | `destino()`: vuelve a la ficha desde la que se abrió el modal, o al listado. |
| `tests/Feature/MarcacionControllerTest.php` | Tres pruebas nuevas: el retorno según el origen, el modal en las dos pantallas y que sin permiso de crear no aparece. |

### Solución
El mismo componente sirve a las dos pantallas y cambia según el contexto:

- **En Marcaciones**: el CI se escribe a mano, y al guardar se vuelve al listado.
- **En la ficha**: el funcionario ya viene dado (va en un campo oculto, se
  muestra solo como dato), y al guardar se vuelve a esa misma ficha.

Dos detalles:

1. **El botón solo aparece si el funcionario está en la base local.** Una
   marcación exige un CI de `personas`; en una ficha de Mamoré sin registro
   local, el alta siempre habría fallado la validación. Mejor no ofrecerla.
2. **Los ids de los campos se sufijan** (`ci-global`, `fecha-ci7633685`…): si dos
   modales terminan en la misma pantalla, las etiquetas no se pisan.

El permiso se comprueba dentro del componente, así ninguna pantalla puede
olvidarse el `@can`.

### Verificación
```bash
php artisan test --compact tests/Feature/MarcacionControllerTest.php
# 29 pruebas, 29 pasadas, 89 aserciones
php artisan test --compact
# 320 pruebas, 314 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 17 — Licenciar al funcionario desde su ficha, en un modal

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(licencias): modal de alta acotado al funcionario en su ficha`

### Problema
«Registrar licencia» desde la ficha llevaba a la pantalla **«Licenciar»**
completa: los tres alcances (un funcionario / varios / todos), el combo de
búsqueda, la ficha de datos personales repetida y **los 40 turnos** del
funcionario. Todo eso para algo que ya estaba decidido: se licencia **a esa
persona**, y solo importan sus turnos vigentes.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/components/modal-licencia.blade.php` | **Nuevo.** Modal con el alcance ya resuelto (`modo=uno` + el CI en campos ocultos), rango, turno completo u horas, goce y motivo. Sus **turnos vigentes** se cargan por AJAX al abrirlo, reusando el endpoint de la solapa de turnos. |
| `resources/views/funcionarios/paneles.blade.php` | La solapa de licencias abre el modal en vez de ir a la pantalla general. |
| `app/Http/Controllers/LicenciaController.php` | `destino()`: vuelve a la ficha desde la que se abrió el modal; sin origen, al listado filtrado por el carnet, como antes. |
| `resources/views/layouts/app.blade.php` | Estilos del bloque de turnos dentro del modal (alto máximo y scroll propio). |
| `tests/Feature/LicenciaControllerTest.php` | Dos pruebas nuevas: el modal de la ficha (alcance resuelto, turnos vigentes, sin los modos generales) y el retorno según el origen. |

### Solución
El modal hace **una sola cosa**: licenciar a este funcionario. No hay combo ni
modos, y el listado de turnos es solo el de los **vigentes** —que es lo único
licenciable hoy—, cargado por AJAX contra el mismo endpoint que ya usa la solapa
de turnos, y con su propio scroll para que el modal no crezca con quien tiene
muchos.

La pantalla «Licenciar» **se mantiene**: sigue siendo la vía para los feriados
(modo «todos») y las altas en lote (modo «varios»), que no tienen lugar en una
ficha individual.

Al guardar se vuelve a la ficha. Si la validación rebota o no hubo ningún turno
en el rango, el modal se reabre solo con los datos cargados y el aviso a la
vista, en vez de dejar al usuario frente a un formulario en blanco.

### Verificación
```bash
php artisan test --compact tests/Feature/LicenciaControllerTest.php
# 56 pruebas, 56 pasadas, 202 aserciones
php artisan test --compact
# 322 pruebas, 316 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 18 — Eliminar licencias desde la ficha, sin perder la solapa

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(licencias): eliminar desde la solapa de la ficha y recordar la solapa abierta`

### Problema
En la solapa de licencias de la ficha se podían **anotar** pero no **borrar**:
para dar de baja una licencia mal cargada había que ir al listado general y
buscarla ahí. El botón sí existía en ese listado, con el modal de baja del
Trabajo 12.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/funcionarios/licencias-list.blade.php` | Columna de acciones con `<x-boton-eliminar>`, el mismo modal global que pide el motivo y deja la baja auditada. Solo para quien tiene el permiso. |
| `resources/views/funcionarios/paneles.blade.php` | La solapa abierta queda en el ancla de la URL (`#licencias`) y se restaura al cargar. |
| `app/Http/Controllers/LicenciaController.php` | Al volver a la ficha, la URL lleva `#licencias`. |
| `tests/Feature/LicenciaControllerTest.php` | Dos pruebas nuevas: la baja desde la solapa y que sin permiso el botón no aparece. |

### Solución
La baja reusa el **modal global de eliminación**: mismo pedido de motivo y misma
auditoría (quién borró y por qué) que el resto del sistema. El botón se dibuja
solo si el usuario puede borrar esa licencia.

El detalle que hacía falta resolver: el modal manda un `DELETE` normal y la
respuesta **recarga la ficha**, que hasta ahora volvía siempre a la primera
solapa. Ahora la solapa abierta queda en el ancla de la URL y se restaura al
cargar, así borrar (o anotar) una licencia deja al usuario donde estaba.

### Verificación
```bash
php artisan test --compact tests/Feature/LicenciaControllerTest.php
# 58 pruebas, 58 pasadas, 209 aserciones
php artisan test --compact
# 324 pruebas, 318 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 19 — Asignar turno en modal, y un marcador para saber qué formulario rebotó

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(turnos): asignar en modal desde la ficha y distinguir el formulario que rebotó`

### Problema
«Asignar turno» desde la ficha llevaba al formulario `turnos-asignados/create`,
que trae el combo para **elegir el funcionario**: un paso de más cuando ya se
está parado en su ficha. Igual que pasaba con las licencias antes del Trabajo 17.

Y apareció un problema propio de tener varios formularios en una misma pantalla:
los tres modales de la ficha comparten nombres de campo (`ci`, `desde`,
`hasta`…), así que la regla «si hay errores, reabrí el modal» hacía que un
rechazo en licencias **también** abriera el de turnos.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/components/modal-turno-asignado.blade.php` | **Nuevo.** Turno (agrupado por día), rango y observación, con el CI y el origen en campos ocultos. |
| `resources/views/funcionarios/paneles.blade.php` | La solapa de turnos abre el modal en vez de ir al formulario general. |
| `resources/views/components/modal-{licencia,marcacion}.blade.php` | Suman el marcador `_form` y lo usan para decidir si reabrirse. |
| `app/Http/Controllers/AsignacionTurnoController.php` | Al volver a la ficha, la URL lleva `#turnos`. |
| `tests/Feature/AsignacionTurnoControllerTest.php` | Prueba nueva del modal (alcance resuelto y turnos agrupados) y el ancla en el retorno. |

### Solución
Cada formulario manda ahora un campo oculto `_form` con su nombre
(`turno-asignado`, `licencia`, `marcacion`), y cada modal se reabre **solo si el
rebote fue suyo**. Es la forma más chica de desambiguar: la alternativa —bolsas
de errores propias por formulario— obligaba a tocar también las pantallas
generales, que siguen usando la bolsa por defecto.

El select de turnos se arma en el componente, agrupado por día de la semana y
con el horario a la vista. Es una consulta liviana y solo se hace para quien
tiene permiso de asignar.

El formulario `turnos-asignados/create` **se mantiene**: es la vía cuando se
entra por el listado y todavía hay que elegir a la persona.

### Verificación
```bash
php artisan test --compact tests/Feature/AsignacionTurnoControllerTest.php
# 20 pruebas, 20 pasadas, 82 aserciones
php artisan test --compact
# 325 pruebas, 319 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

> **Nota menor del mismo tramo:** la solapa de marcaciones pasó a mostrar **10
> registros por página** por defecto (antes 25), como el resto de los listados.

---

## Trabajo 20 — Concluir un turno (y eliminarlo solo si se cargó mal)

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(turnos): concluir una asignación con fecha de fin, y baja para lo cargado por error`

### Problema
Una asignación de turno se podía crear pero no cerrar. Cuando un funcionario
cambiaba de horario, la única salida a mano era **borrar** la asignación… y con
eso se perdía la explicación de sus marcaciones y licencias de ese período: por
qué entraba 05:30 el año pasado si «nunca» tuvo ese turno.

### Solución: son dos acciones distintas
| Acción | Cuándo | Qué hace |
|---|---|---|
| **Concluir** | El funcionario **dejó** ese turno (cambió de horario, se fue de la unidad) | Le pone `hasta` = la fecha elegida. Pasa a «Vencida» y **la historia queda** |
| **Eliminar** | La asignación **se cargó mal** (turno o cédula equivocada) | Baja lógica con motivo y auditoría: el registro nunca debió existir |

Concluir es la acción de todos los días, así que es la que aparece primero; la
baja queda como la excepción, y su aviso lo dice: «Si el funcionario dejó ese
turno, concluilo en vez de borrarlo». **Concluir solo se ofrece en las que no
están vencidas**: cerrar algo que ya terminó no tiene sentido.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Requests/ConcluirAsignacionTurnoRequest.php` | **Nuevo.** Valida la fecha de fin contra el inicio de esa asignación y autoriza en el propio request. |
| `app/Http/Controllers/AsignacionTurnoController.php` | `concluir()` y `destroy()`. Al concluir desde una ficha se vuelve a su solapa de turnos. |
| `app/Policies/AsignacionTurnoPolicy.php` | Habilidades `update` (concluir) y `delete`, sobre los permisos de turnos. |
| `routes/web.php` | `PATCH …/{asignacion}/concluir` y `DELETE …/{asignacion}`. |
| `resources/views/components/boton-concluir.blade.php` | **Nuevo.** Botón que carga el modal global, como el de eliminar. |
| `resources/views/layouts/app.blade.php` | Modal global de «Concluir» (store de Alpine + un solo formulario, para que sirva a las tablas que llegan por AJAX). El de eliminación acepta un ancla. |
| `resources/views/components/boton-eliminar.blade.php` | Prop `ancla`: la solapa desde la que se borró, para volver ahí. |
| `resources/views/{funcionarios/turnos-list,turnos-asignados/list}.blade.php` | Columna de acciones en las dos tablas. |
| `app/Http/Controllers/PersonaController.php` | La solapa de turnos pasa su origen, para que los botones sepan a dónde volver. |
| `tests/Feature/AsignacionTurnoControllerTest.php` | Seis pruebas nuevas (26 en el archivo). |

### Detalles que vale anotar
1. **Concluir con fecha de hoy deja la asignación vigente hoy y vencida mañana.**
   Es lo correcto: el último día trabajado cuenta. La prueba viaja un día al
   futuro para verificarlo.
2. **El modal vive en el layout, no en la fila.** Los botones de las tablas se
   inyectan por AJAX, así que el estado va en un store de Alpine —igual que el
   modal de baja del Trabajo 12— y hay un solo formulario en toda la pantalla.
3. **La baja vuelve a la solapa desde la que se hizo** gracias al ancla que
   ahora acepta el botón de eliminar.

### Verificación
```bash
php artisan test --compact tests/Feature/AsignacionTurnoControllerTest.php
# 26 pruebas, 26 pasadas, 102 aserciones
php artisan test --compact
# 331 pruebas, 325 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Trabajo 21 — El turno se elige viendo su horario, no adivinando

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(turnos): selector de turno con horario, jornada y tolerancia a la vista`

### Problema
El modal de asignación tenía un `<select>` nativo: una lista de nombres
(«LUN: 08:00 - 16:00») agrupada por día. Dos problemas:

- **El nombre no es el turno.** Es un texto libre que alguien escribió al
  crearlo; puede estar desactualizado o no decir nada. Las horas de tolerancia,
  la jornada y si la salida cae al día siguiente **no se veían en ninguna parte**
  hasta después de asignar.
- Con 46 turnos cargados, un desplegable de 46 líneas obliga a recorrerlas todas.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/components/modal-turno-asignado.blade.php` | El `<select>` pasa a ser una lista de tarjetas seleccionables, filtrable por día, y un resumen del turno elegido. |
| `resources/views/layouts/app.blade.php` | Estilos del selector (`.turno-picker`, `.chip`, `.turno-opcion`). |
| `tests/Feature/AsignacionTurnoControllerTest.php` | La prueba del modal verifica el selector nuevo. |

### Solución
Cada turno es una **tarjeta** que muestra lo que hace falta para decidir:

- El día abreviado y el nombre del turno.
- El horario grande: `08:00 → 16:00`.
- Debajo, en chico: las horas de jornada, hasta qué hora hay tolerancia de
  entrada y una etiqueta **«Sale al día siguiente»** cuando corresponde.

Arriba, unos chips filtran por día de la semana —solo aparecen los días que
tienen turnos cargados— y abajo queda una línea que confirma en palabras lo
elegido, para que no haya que volver a mirar la lista antes de guardar.

Debajo del capó siguen siendo `input type="radio"` con el mismo `name="turno_id"`
y el atributo `required`: la validación del navegador y la del servidor no
cambian, y sin turnos cargados el modal avisa con el enlace para crear el
primero.

### Verificación
```bash
php artisan test --compact tests/Feature/AsignacionTurnoControllerTest.php
# 26 pruebas, 26 pasadas, 102 aserciones
php artisan test --compact
# 331 pruebas, 325 pasadas, 6 falladas (las de DiaExcepcional/DiaTurno, del 26/07)
vendor/bin/pint --dirty --format agent
# passed
```

---

## Informe para presentación (copiar/pegar en Word)

**Fecha:** 28 de julio de 2026
**Responsable:** nachoDev

**Qué se hizo:** Se agregó al listado de funcionarios del sistema biométrico un
selector que permite ver **todo el personal**, **solo los funcionarios con
contrato firmado** o **solo las personas que no tienen contrato**.

**Por qué hacía falta:** Hasta ahora el listado mezclaba todo. El sistema tenía
registradas 4.595 personas, de las cuales 1.040 son funcionarios en funciones con
contrato firmado y 3.555 no lo son. No había forma de distinguirlos desde la
pantalla.

**Cómo funciona:**
- Al elegir **«Con contrato»**, la tabla muestra únicamente al personal en
  funciones y agrega dos columnas: el **cargo** y la **dirección administrativa**
  a la que pertenece cada funcionario.
- Al elegir **«Sin contrato»**, muestra a las personas registradas que hoy no
  tienen contrato firmado.
- En **«Todos»**, cada nombre lleva una etiqueta de color que indica de un
  vistazo si tiene contrato o no.
- Cada opción del selector muestra **cuántas personas hay** en esa situación:
  «Con contrato (1.040)», «Sin contrato (3.555)».
- El buscador por cédula o nombre sigue funcionando dentro de cualquiera de las
  tres opciones, y los números del selector se ajustan a lo que se busca.

**Trabajo sobre el sistema de personal:** Para que esto fuera posible hubo que
ampliar el servicio de datos del sistema de Recursos Humanos, que hasta ahora
entregaba las personas y los contratos por separado sin forma de cruzarlos.
Hacerlo desde el sistema biométrico habría exigido descargar las dos listas
completas —unas 57 consultas seguidas, por encima del límite permitido— y aun así
los datos habrían quedado desactualizados. Con el cambio, la consulta se resuelve
en el servidor de personal y el sistema biométrico pide **una sola vez** lo que
necesita mostrar.

Aprovechando el mismo trabajo se agregaron al servicio dos mejoras: la **lista de
direcciones y unidades administrativas** (con cuántos funcionarios dependen de
cada una), y un **buscador** que ahora también encuentra por cargo, denominación
del puesto y código de contrato.

**Simplificación del servicio:** Al terminar se revisó lo construido y se lo
redujo. El servicio había quedado con **8 consultas distintas repartidas en 7
archivos**; se dejó en **4 consultas y 2 archivos**, con la misma información y
sin perder ninguna función. La consulta principal es una sola lista con **todas
las personas**: quien tiene contrato viaja con los datos de su contrato, quien no
lo tiene aparece igual en la lista. Los dos manuales del servicio (1.346 líneas)
se unificaron en uno de 688. Menos piezas para mantener y menos para explicarle a
quien lo conecte.

**El cargo, en todo el sistema:** Ya que el servicio entrega el cargo junto con
cada persona, se aprovechó en todas las pantallas que muestran funcionarios:

- La **ficha del funcionario** suma un recuadro «Contrato vigente» con el cargo,
  la denominación del puesto, la dirección y la unidad administrativa, el código
  del contrato, el tipo de proceso y la vigencia. Si la persona no tiene
  contrato, lo dice claramente.
- Los **buscadores de funcionario** (licencias y reportes) ahora muestran
  «cédula — nombre — cargo (dirección)», así se distingue al toque entre
  homónimos.
- Los **listados de marcaciones y de licencias** muestran el cargo debajo del
  nombre.
- El **reporte de marcaciones** imprime el cargo y la dirección del funcionario,
  conservando exactamente el formato que ya se usaba.
- El **buscador del reporte**, que hasta ahora leía de la base vieja del sistema
  de asistencia, pasó a leer del sistema de personal, con la base vieja como
  respaldo por si el servicio no responde.

**Las marcaciones, dentro de la ficha:** La ficha del funcionario mostraba sus
datos y su contrato, pero para saber a qué hora marcó había que salir a otra
pantalla y volver a buscarlo. Ahora la propia ficha incluye el detalle de sus
marcaciones, con filtros por fecha desde/hasta y por tipo de marcación, la
cantidad de registros a mostrar por página y el botón para imprimir el reporte
con esos mismos filtros. La tabla se actualiza sola al cambiar un filtro, sin
recargar la pantalla, igual que los demás listados del sistema.

**El menú indica dónde se está:** Al abrir una pantalla, el menú lateral no
mostraba de ninguna manera qué opción estaba abierta. El caso más molesto era el
de **Horarios** y **Licencias**: como están dentro del grupo «Parámetros» y ese
grupo se abría solo para «Días excepcionales», al entrar a cualquiera de las dos
el menú quedaba cerrado y sin ninguna señal. Ahora la opción actual se resalta
con negrita y una barra verde, el grupo que la contiene se abre solo, y el
resaltado se mantiene mientras se navega dentro del módulo (por ejemplo, al abrir
la ficha de un funcionario sigue marcado «Funcionarios»).

**Más espacio en pantalla:** El botón de las tres rayas de la barra superior solo
funcionaba en celulares y tabletas. Ahora también en computadora: encoge el menú
lateral a una **franja angosta con los iconos**, y la tabla gana ese ancho —útil
en los listados con muchas columnas—. Los iconos siguen a mano, con el nombre de
cada opción al pasar el mouse, y el sistema recuerda la preferencia mientras se
navega.

**Barra superior:** Se quitó el enlace **«Volver al panel»**, que llevaba a una
pantalla que ya no existe desde que se retiró el panel anterior. En su lugar la
barra muestra ahora **con qué usuario se está trabajando** (foto o iniciales,
nombre, correo y rol) y desde ahí se **cierra la sesión**, con un botón claro y
diferenciado en lugar del «Salir» en letra chica.

**Un solo nombre para cada cosa:** La pantalla de «Horarios» ahora se llama
**«Turnos»**, que es como el personal y el propio sistema llaman a esos
registros. Antes convivían las dos palabras —el menú decía «Horarios» y la tabla
mostraba una columna «Horario» con el nombre del turno—, lo que confundía a quien
recién empieza a usarlo. La dirección web no cambió, así que los accesos directos
guardados siguen funcionando.

**Turnos asignados:** El sistema guardaba qué turno tiene asignado cada
funcionario y desde cuándo hasta cuándo, pero no había pantalla para verlo: solo
se podían consultar los turnos por un lado y los funcionarios por otro. Ahora hay
un listado que los cruza, con el nombre y el cargo del funcionario, su turno con
los horarios de entrada y salida, el período de vigencia y una etiqueta que
indica si la asignación está **vigente**, **vencida** o **aún no empieza**. Se
puede buscar por cédula, nombre o turno, y filtrar por día de la semana o por
situación. Las asignaciones que quedaron sin turno válido al copiar los datos del
sistema anterior aparecen marcadas, para poder corregirlas.

**Eliminar deja constancia:** Hasta ahora, borrar un registro mostraba el cartel
gris del navegador («¿Eliminar?», Aceptar/Cancelar), que no decía qué se estaba
por borrar ni permitía anotar el motivo. Se reemplazó por una **ventana de
confirmación única para todo el sistema**: describe exactamente qué registro se
elimina, **pide escribir por qué** y exige marcar una casilla antes de habilitar
el botón rojo. El sistema guarda junto a cada baja **quién la hizo y el motivo**,
de modo que después se puede saber por qué desapareció un registro. Los datos no
se borran de la base: quedan marcados como eliminados.

**Los turnos, en la ficha del funcionario:** El listado de turnos asignados
responde «quién tiene este turno», pero lo que se consulta a diario es lo
contrario: qué turno tiene una persona. Ahora la ficha del funcionario lo muestra
directamente, con el horario de entrada y salida, el día y el período de
vigencia. Las asignaciones **vencidas** no molestan a la vista —aparecen con un
botón «Ver también las vencidas»—, así se puede revisar qué turno tuvo antes sin
salir de la pantalla. Funciona igual en la ficha del sistema de personal y en la
del sistema de asistencia.

**Asignar turnos desde el sistema:** Hasta ahora los turnos de cada funcionario
solo se podían consultar; asignar uno nuevo requería intervenir la base de datos.
Ahora se hace desde la pantalla: en la ficha del funcionario aparece el botón
**«Asignar turno»** —con la persona ya elegida— y basta indicar el turno y desde
cuándo hasta cuándo rige. Los turnos se eligen de una lista agrupada por día de
la semana, con su horario a la vista. Si esa persona ya tenía asignado ese mismo
turno desde esa fecha, el sistema lo avisa en lugar de duplicarlo.

**La ficha, más ordenada:** La ficha del funcionario había quedado con tres
tablas una debajo de la otra —marcaciones, licencias y turnos—, y para llegar a
la última había que recorrer toda la pantalla. Ahora las tres están en
**solapas**: se elige cuál mirar con un clic, cada una tiene sus propios filtros,
su cantidad de registros por página y su paginación, y la información se carga
sola sin recargar la pantalla.

**Anotar una marcación donde se la necesita:** Registrar una marcación a mano
—cuando el reloj no la tomó— solo se podía desde la pantalla de Marcaciones,
escribiendo la cédula. Ahora el mismo formulario está también en la ficha del
funcionario, dentro de la solapa de marcaciones y con la persona ya cargada: se
indican fecha y hora y listo, y la pantalla vuelve a la ficha.

**Licenciar sin dar vueltas:** Desde la ficha, «Registrar licencia» abría la
pantalla general de licencias, con la búsqueda de funcionario, los tres alcances
(uno, varios, todos) y la lista completa de turnos —incluidos los vencidos—,
cuando en realidad ya estaba definido a quién se licencia. Ahora se abre una
ventana acotada a esa persona, que muestra únicamente sus **turnos vigentes** y
pide lo necesario: desde cuándo, hasta cuándo, si es turno completo o por horas,
si es con goce y el motivo. La pantalla general sigue disponible para los
feriados y las licencias en lote.

Desde la misma solapa también se puede **eliminar** una licencia mal cargada,
con la misma ventana de confirmación que el resto del sistema —pide el motivo de
la baja y queda registrado quién la hizo—, sin tener que ir al listado general a
buscarla.

**Asignar turnos igual de directo:** «Asignar turno» desde la ficha también abre
ahora una ventana, en lugar de llevar a un formulario donde había que volver a
buscar al funcionario. Se elige el turno de una lista agrupada por día de la
semana, con su horario a la vista, y desde cuándo hasta cuándo rige.

**Cerrar un turno sin borrar la historia:** Faltaba poder dar por terminado un
turno cuando el funcionario cambia de horario. Se agregaron dos acciones bien
separadas, porque son cosas distintas:

- **Concluir**: se elige hasta qué día rige y la asignación queda cerrada. No se
  borra nada, así el sistema sigue pudiendo explicar por qué esa persona entraba
  a una hora distinta el año pasado, cuando se revisen sus marcaciones o
  licencias viejas.
- **Eliminar**: solo para lo que se cargó por error (turno o cédula
  equivocada). Pide el motivo y queda registrado quién lo hizo.

El aviso de la eliminación lo dice expresamente: si el funcionario dejó el turno,
lo que corresponde es concluirlo, no borrarlo.

**Elegir el turno sabiendo cuál es:** Al asignar, el turno se elegía de una lista
desplegable que solo mostraba su nombre; el horario real, las horas de jornada y
la tolerancia de entrada recién se veían después de asignarlo. Ahora cada turno
se presenta como una ficha con su **horario en grande** (por ejemplo 08:00 →
16:00), las horas de jornada, hasta qué hora hay tolerancia y un aviso cuando la
salida cae al día siguiente. Además se puede filtrar por día de la semana, útil
con los más de 40 turnos que hay cargados, y antes de guardar se confirma en
palabras cuál quedó elegido.

**Resultado:** El personal de Recursos Humanos puede separar en un clic al
personal contratado del que no lo está, ver el cargo y la dirección de cada
funcionario en cualquier pantalla del sistema —sin consultas adicionales—,
revisar las marcaciones y los turnos de una persona sin salir de su ficha, saber
en todo momento en qué sección del sistema está, y dar de baja un registro
dejando escrito el motivo. Se agregaron 54 pruebas automáticas nuevas; las
pruebas de los módulos afectados pasan.

---

## Lo que sigue (próximos pasos)

- **Filtro por dirección y unidad en el listado de SISBIO.** Del lado de la API ya
  está todo: `GET /catalogos` da las 46 direcciones y sus unidades, y `/people`
  acepta `?direccion=&unidad=`. Falta solo el lado de SISBIO: dos combos
  encadenados. Con eso RRHH podría pedir «los funcionarios de la Dirección de
  Sistemas» directo desde el listado.
- **Buscar por cargo desde SISBIO.** La API ya busca por cargo, denominación y
  código de contrato, pero el buscador del listado sigue etiquetado «Buscar por CI
  o nombre…». Conviene avisar que también encuentra por cargo.
- **Las 6 pruebas de `DiaExcepcional` y `DiaTurno`** siguen apuntando al listado
  en vez del parcial AJAX. Vienen del 26/07 y no las tocó nada de hoy.
- **Motivo de baja obligatorio del lado del servidor.** Hoy solo Equipos lo
  valida; en el resto lo exige el modal, así que un `DELETE` armado por fuera
  guarda la baja sin motivo. Si se quiere cerrar, el lugar es una regla en el
  `deleting` del trait o un middleware para las peticiones `DELETE`.
- **Las dos migraciones de auditoría de hoy** (`users` y `roles`) se pueden
  fusionar en una sola antes de commitear; implica rehacerlas con
  `migrate:rollback --step=2`.
- **Base de pruebas en sysadmin.** Ese proyecto no tiene una base de pruebas
  configurada, así que la API no se puede cubrir con pruebas automáticas. Es el
  pendiente más importante del lado de Mamoré.
- **Límite de peticiones y sincronización.** El tope de 60 por minuto por IP
  frena cualquier sincronización masiva. Si alguna vez hace falta, la API
  necesita un `?updated_since=` o soporte de ETag.

---

*Documento de sesión — nachoDev — Gobierno Autónomo Departamental del Beni.*
