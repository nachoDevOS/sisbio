# Sesión de trabajo — 2026-07-24

> Registro de trabajo realizado por **nachoDev**. Rama: `master`.
>
> Migración del sistema de la base de datos legada **SIA (SQL Server 2008 R2)**
> a la **MySQL local** del sistema. Trabajo iniciado el 2026-07-23 por la noche y
> cerrado el 2026-07-24 (commit «nuevo sistema migrado»). Cinco trabajos: (1) la
> nueva base MySQL espejo del SIA, (2) los comandos que copian los datos tabla por
> tabla, (3) el seeder que orquesta toda la migración, (4) el aislamiento de las
> pruebas frente al SQL Server y (5) el «flip» de la aplicación para que lea de
> MySQL en vez del SIA.

---

## Trabajo 1 — Nueva base MySQL local (modelos + migraciones espejo del SIA)

**Commit:** `d22a82b` — `nuevo sistema`

### Problema
El sistema dependía por completo del SQL Server legado (SIA) para leer personas,
marcaciones, profesiones y horarios. Se necesitaba que SISBIO fuera dueño de sus
datos en su propia base MySQL, replicando los campos del SIA tal cual (mismos
nombres, longitudes y nulabilidad).

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Models/Persona.php` | Modelo local (tabla `personas`, `IdPersona`→`ci`), SoftDeletes, `$fillable` camelCase, relaciones. |
| `app/Models/Asistencia.php` | Modelo local de marcaciones (tabla `asistencias`, col `ci`). |
| `app/Models/Profesion.php`, `app/Models/Turno.php` | Modelos locales de catálogos (`profesiones`, `turnos`). |
| `database/migrations/*_create_personas_table.php` | Tabla espejo: id Laravel + timestamps + softDeletes; los 16 campos del SIA idénticos; `ci` `unique()`. |
| `database/migrations/*_create_{asistencias,profesions,turnos}_table.php` | Tablas espejo de marcaciones, profesiones y turnos. |

### Solución
Se crearon las tablas MySQL locales replicando el esquema del SIA (misma
identidad de campos, `char()` para longitud fija) pero con la convención del
sistema: id autoincremental de Laravel, timestamps y borrado lógico
(softDeletes). La clave del SIA (`IdPersona`) pasa de PK a columna `unique()` y se
renombra a `ci`; las tablas y columnas quedan en convención local (tablas en
minúscula plural, columnas camelCase). Los modelos legados `App\Models\Sia\*`
(conexión `sqlsrv`, solo lectura) siguen existiendo para las piezas aún no
migradas.

---

## Trabajo 2 — Comandos de copia SIA → MySQL (tabla por tabla)

**Commit:** `d22a82b` · `9828552` · `a39e6d8` · `681d04c` · `c9a2fc2` — `nuevo sistema`

### Problema
Había que trasladar los datos reales del SQL Server a MySQL sin tocar el origen
(sigue solo-lectura), de forma repetible y sin duplicar filas al reejecutar.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Console/Commands/MigrarPersonasSia.php` | `sia:migrar-personas` — copia Personas→`personas`. |
| `app/Console/Commands/MigrarMarcacionesSia.php` | `sia:migrar-marcaciones` — Asistencia→`asistencias` (~4.4M filas). |
| `app/Console/Commands/MigrarProfesionesSia.php` | `sia:migrar-profesiones` — catálogo. |
| `app/Console/Commands/MigrarHorariosSia.php` | `sia:migrar-horarios` — DiaTurnos→`turnos`. |
| `app/Console/Commands/MigrarLicenciasSia.php` | `sia:migrar-licencias` — Licencias→`licencias` (+ FK `turno_id`). |
| `app/Console/Commands/MigrarAsignacionTurnosSia.php` | `sia:migrar-asignacion-turnos` (+ FK `turno_id`). |
| `app/Console/Commands/MigrarDiasExcepcionalesSia.php` | `sia:migrar-dias-excepcionales` — Calendario→`dias_excepcionales`. |
| `app/Models/{Licencia,AsignacionTurno,DiaExcepcional}.php` | Modelos locales de las tablas nuevas. |
| `database/migrations/*_create_{licencias,asignacion_turnos,dia_excepcionals}_table.php` | Tablas espejo nuevas. |

### Solución
Siete comandos, todos **idempotentes** (upsert por la clave del SIA): recortan el
padding de los `char()`, pueblan `created_at`/`updated_at` y **no tocan el
origen**. Cada comando mapea SIA→local con un array `MAPA` const (PascalCase del
SQL Server → camelCase local). Las tablas grandes (personas, marcaciones,
licencias, asignacion-turnos, dias-excepcionales) usan **`cursor()`** en vez de
`chunk()`: en SQL Server 2008 el `chunk()` pagina con `ROW_NUMBER` (O(n²), parece
colgado); los catálogos chicos (profesiones, horarios) siguen con `chunk()`.
`licencias` y `asignacion_turnos` además llevan una FK **`turno_id`→`turnos.id`**
que el comando resuelve cruzando el `idTurno` contra la tabla `turnos` local (por
eso `turnos` debe migrarse antes).

### Verificación
```bash
php artisan test --compact --filter="MigrarPersonasSia|MigrarMarcacionesSia|MigrarProfesionesSia|MigrarHorariosSia|MigrarLicenciasSia|MigrarAsignacionTurnosSia|MigrarDiasExcepcionalesSia"
```

---

## Trabajo 3 — Seeder maestro que orquesta toda la migración

**Commit:** `a39e6d8` · `c9a2fc2` · `681d04c` — `nuevo sistema`

### Problema
Correr la migración a mano era propenso a errores de orden (las FK exigen migrar
`turnos` antes que licencias/asignaciones) y no dejaba el sistema en un estado
limpio y reproducible.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `database/seeders/MigrarSiaSeeder.php` | Orquestador: `migrate:fresh --seed` + los 7 comandos de copia en orden. |

### Solución
`php artisan db:seed --class=MigrarSiaSeeder` primero corre `migrate:fresh --seed`
(borra todo, recrea el esquema y el usuario/roles) y luego ejecuta los **7**
comandos de copia en el orden correcto (profesiones, personas, horarios,
marcaciones, licencias, asignacion-turnos, dias-excepcionales — horarios antes de
licencias/asignaciones por la FK). En entorno de pruebas **no** hace
`migrate:fresh` (rompería `RefreshDatabase`). No está en `DatabaseSeeder`: se
corre aparte, a propósito.

> **Entornos:** el código se edita en Linux; la migración real la corre el usuario
> en Windows (con `pdo_sqlsrv` y red al SIA). Son checkouts distintos → hay que
> commit + push + pull.

---

## Trabajo 4 — Aislamiento de las pruebas frente al SQL Server

**Commit:** `d22a82b` · `9828552` · `a39e6d8` · `681d04c` — `nuevo sistema`

### Problema
Con `pdo_sqlsrv` instalado, cualquier test que renderizara el tablero sin falsear
la conexión `sia` pegaba al **SQL Server real** y se colgaba por login timeout. El
suite tardaba ~124 s en vez de ~3 s.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `tests/Pest.php` | `beforeEach` global que apunta `sia`→sqlite vacío en todos los tests Feature, y helper `fakeSiaDatabase()` que crea las tablas del SIA falso. |
| `tests/Feature/Migrar*SiaTest.php` | Un test por comando de copia (siembran el SIA falso y verifican el upsert). |

### Solución
Se añadió un `beforeEach` global en `tests/Pest.php` que redirige la conexión
`sia` a un sqlite en memoria para todos los tests, y un helper `fakeSiaDatabase()`
que crea las tablas del origen falso. Así ningún test toca el SQL Server real y el
suite vuelve a correr en segundos. Al agregar una tabla nueva a la migración hay
que sumar su tabla a `fakeSiaDatabase()` y su comando a `MigrarSiaSeeder`.

---

## Trabajo 5 — «Flip»: la aplicación lee de MySQL local

**Commit:** `2a3ddd3` — `nuevo sistema migrado`

### Problema
Aunque los datos ya estaban en MySQL, la aplicación seguía **leyéndolos del SIA**
(SQL Server) en las pantallas de Marcaciones, Horarios, Funcionarios y Reportes.
Había que cambiarla para que consumiera las tablas locales.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/MarcacionController.php` | Lee `App\Models\Asistencia` local (cols `fecha`/`hora`/`tipo`). |
| `app/Http/Controllers/DiaTurnoController.php` | CRUD de horarios lee/escribe `App\Models\Turno` local. |
| `app/Http/Controllers/PersonaController.php` | Funcionarios sobre `App\Models\Persona` local (URLs por `ci`). |
| `app/Http/Controllers/ReporteMarcacionController.php` | Reporte lee Persona/Asistencia/Profesion locales. |
| `app/Policies/{Asistencia,Turno,Persona}Policy.php` | Policies nuevas para los modelos locales (mismos permisos). |
| `app/Http/Requests/{Store,Update}PersonaRequest.php` | Reglas en camelCase (`unique('personas','ci')`, `exists('profesiones','codigoProfesion')`). |
| `database/factories/{Persona,Asistencia,Profesion,Turno}Factory.php` | Factories locales (camelCase). |
| `resources/views/{funcionarios,horarios,marcaciones,reportes}/**` | Vistas migradas a camelCase (`CorreoE`→`correo`, etc.). |
| `tests/Feature/{Marcacion,DiaTurno,Persona,ReporteMarcacion}ControllerTest.php` | Los tests de display siembran tablas locales; los de import siguen sembrando el SIA. |

### Solución
Se apuntaron los controllers de Marcaciones, Horarios, Funcionarios y Reportes a
los modelos MySQL locales, con sus policies, factories y requests nuevos. `Persona`
usa `getRouteKeyName()='ci'` (URLs por carné). Las vistas pasaron a camelCase.
Queda **pendiente** el flip del `DashboardController` (aún usa `Sia\*`) y del
**import** de CSV / `EquipoController::sincronizarMarcaciones`, que todavía
escriben al SIA vía `RegistroAsistenciaSia`; hasta migrarlos, lo importado no
aparece en el listado local hasta re-migrar.

### Verificación
```bash
php artisan test --compact --filter="MarcacionControllerTest|DiaTurnoControllerTest|PersonaControllerTest|ReporteMarcacionControllerTest"
```

---

---

# Segunda parte del día — ajustes de acceso, borrado lógico y módulo «Días excepcionales»

> Bloque de trabajo posterior al «flip». Doce trabajos más: (6) usuario de
> acceso por defecto, (7) Funcionarios de solo lectura, (8) borrado lógico en
> todo el sistema (incluye la corrección de un bug de migración), (9)
> homogeneizar los `$fillable` con la base, (10) el módulo nuevo **Días
> excepcionales** bajo un menú **Parámetros**, (11) la corrección del error 500
> al borrar, (12) la auditoría (quién registró/eliminó) en todos los modelos y
> (13) los filtros de tabla estilo DataTables en todos los listados, (14) el
> guardado de las marcaciones del biométrico en la base MySQL local, (15) el
> alta manual de marcaciones tipo M, (16) el listado de funcionarios desde dos
> fuentes (API Mamoré / base local SIAT) y (17) el nombre del funcionario en
> Marcaciones resuelto por Mamoré con fallback a la base local.

---

## Trabajo 6 — Usuario de acceso por defecto (`admin@admin.com`)

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(seed): usuario admin por defecto admin@admin.com`

### Problema
El usuario semilla del sistema era `test@example.com`, poco claro para entrar a
demostrar el sistema.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `database/seeders/DatabaseSeeder.php` | Usuario semilla → `Admin` / `admin@admin.com` con rol `super_admin`. |
| `tests/Feature/RolesAndPermissionsSeederTest.php` | Verifica el nuevo correo. |

### Solución
El seeder crea el usuario `admin@admin.com` con rol `super_admin`. La contraseña
es `password` (valor por defecto del factory de usuarios, sin cambios). Se entra
con `admin@admin.com` / `password`.

---

## Trabajo 7 — Funcionarios de solo lectura (sin crear ni editar)

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(funcionarios): dejar el módulo en solo lectura`

### Problema
La pantalla de Funcionarios permitía crear y editar, pero el alta/edición de un
funcionario sigue siendo responsabilidad del sistema de escritorio; desde SISBIO
solo debe consultarse.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `routes/web.php` | Recurso `funcionarios` limitado a `index` + `show`. |
| `resources/views/funcionarios/index.blade.php` | Quitados botón «Nuevo funcionario» e ícono editar. |
| `resources/views/funcionarios/show.blade.php` | Quitado botón «Editar». |
| `app/Http/Controllers/PersonaController.php` | Eliminados `create/store/edit/update` y su helper. |
| `resources/views/funcionarios/{create,edit,_form}.blade.php` | Eliminados (código muerto). |
| `app/Http/Requests/{Store,Update}PersonaRequest.php` | Eliminados (ya no se usan). |
| `tests/Feature/PersonaControllerTest.php` | Quitados los 5 tests de alta/edición obsoletos. |

### Solución
Se dejó el recurso solo con `index` y `show`, se quitaron los botones de la
interfaz y se eliminó el código de alta/edición (controlador, vistas, requests y
sus pruebas). El reporte imprimible de marcaciones sigue funcionando.

---

## Trabajo 8 — Borrado lógico en TODO el sistema (+ bug de migración corregido)

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(roles): borrado lógico de roles y fix migración equipos`

### Problema
Se pidió que **toda** eliminación del sistema sea lógica (marca `deleted_at`, no
borra). Los modelos de dominio ya usaban SoftDeletes, pero el modelo **Role**
(de la librería Spatie) borraba físico. Además, al probar en base limpia salió a
la luz un bug: la migración de `equipos` definía `timestamps()` y `softDeletes()`
**dos veces**, lo que rompía la creación de la tabla en un `migrate:fresh`.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Models/Role.php` | **Nuevo:** extiende el Role de Spatie y le agrega SoftDeletes. |
| `config/permission.php` | Apunta `models.role` al nuevo `App\Models\Role`. |
| `app/Http/Controllers/{Role,User}Controller.php`, `app/Providers/AppServiceProvider.php`, `app/Policies/RolePolicy.php`, `app/Http/Requests/UpdateRoleRequest.php` | Usan `App\Models\Role` en vez del de Spatie. |
| `database/migrations/*_add_deleted_at_to_roles_table.php` | **Nueva:** agrega `deleted_at` a la tabla `roles`. |
| `database/migrations/*_create_equipos_table.php` | **Bug fix:** quitados el `timestamps()`/`softDeletes()` duplicados. |
| `tests/Feature/RoleControllerTest.php` | Verifica que eliminar un rol es lógico (queda en la base marcado). |

### Solución
Se creó `App\Models\Role` (subclase del Role de Spatie con SoftDeletes) y se
apuntó la configuración de permisos a ese modelo, así **eliminar un rol solo lo
marca** y desaparece de las consultas, sin borrarlo. El rol `super_admin` sigue
sin poder eliminarse. En paralelo se corrigió la migración de `equipos`, que
duplicaba columnas de fecha/borrado y hacía fallar la creación de la base en
limpio.

### Verificación
```bash
php artisan test --compact --filter="RoleControllerTest|UserControllerTest"
```

---

## Trabajo 9 — `$fillable` alineado con la base (auditoría)

**Commit:** `(pendiente de commit)` — mensaje sugerido: `chore(models): sumar observacion y estado a los fillable`

### Problema
Las tablas de dominio tienen columnas de auditoría (`observacion`, `estado`,
y campos de quién registró/eliminó), pero los modelos no las declaraban como
asignables.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Models/{Equipo,Persona,Asistencia,Turno,Profesion,Licencia,AsignacionTurno,DiaExcepcional}.php` | `$fillable` + `observacion` y `estado`. |

### Solución
Se sumaron **solo** los campos de dominio editables (`observacion`, `estado`) al
`$fillable` de los 8 modelos. Los campos de «quién registró/eliminó»
(`registerUser_id`, `deleteUser_id`, `registerRole`, `deleteRole`,
`deleteObservacion`) se dejaron **fuera** a propósito: deben fijarse desde el
servidor cuando se implemente la auditoría, para que un formulario no pueda
falsificar el autor.

---

## Trabajo 10 — Módulo «Días excepcionales» (menú Parámetros)

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(dias-excepcionales): CRUD bajo menú Parámetros`

### Problema
Faltaba en el sistema la pantalla «Días excepcionales para no controlar
asistencia» (feriados/tolerancias) que existía en el escritorio, con alta,
listado, edición y eliminación, sobre la base MySQL local.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/DiaExcepcionalController.php` | **Nuevo:** CRUD (listar/registrar/editar/eliminar). |
| `app/Policies/DiaExcepcionalPolicy.php` | **Nueva:** permisos `*:DiaExcepcional`. |
| `app/Http/Requests/{Store,Update}DiaExcepcionalRequest.php` | **Nuevas:** validan fecha única por día y motivo obligatorio. |
| `resources/views/dias-excepcionales/{index,create,edit,_form}.blade.php` | **Nuevas:** pantallas del módulo. El listado incluye **buscador** por motivo o fecha. |
| `database/factories/DiaExcepcionalFactory.php` + `app/Models/DiaExcepcional.php` | Factory + `HasFactory` para pruebas. |
| `app/Policies/RolePolicy.php` | `DiaExcepcional` sumado a la matriz de permisos (30 → 35). |
| `routes/web.php` | Recurso `dias-excepcionales` (sin ficha). |
| `resources/views/layouts/app.blade.php` | Menú lateral **Parámetros** (desplegable) → **Días excepcionales**. |
| `tests/Feature/DiaExcepcionalControllerTest.php` | **Nuevo:** 10 pruebas del CRUD + borrado lógico + permisos. |
| `tests/Feature/RolesAndPermissionsSeederTest.php` | Conteo de permisos 30 → 35. |

### Solución
Se agregó al menú lateral un grupo desplegable **Parámetros** y dentro **Días
excepcionales**, con CRUD completo sobre la tabla local `dias_excepcionales`:
listar, registrar, editar y eliminar (borrado lógico). El listado tiene un
**buscador** que filtra por motivo (texto parcial) o por fecha (año, `YYYY-MM-DD`
o `dd/mm/YYYY`). Cada fecha es única (una fila por día). El acceso se controla
con permisos propios (`*:DiaExcepcional`), que se suman a la matriz de la
pantalla de Roles.

### Verificación
```bash
php artisan test --compact --filter="DiaExcepcionalControllerTest"
```

---

## Trabajo 11 — Fix: borrar un día excepcional daba error 500 (auditoría)

**Commit:** `(pendiente de commit)` — mensaje sugerido: `fix(auditoria): trait RegistersUserEvents robusto al borrar`

### Problema
Al eliminar un día excepcional la app tiraba **500**:
`SQLSTATE[42S22]: Column not found: 1054 Unknown column 'deleteObservacion'`.
El modelo usa el trait `RegistersUserEvents`, que al borrar graba quién eliminó;
el trait usaba un nombre de columna con la ortografía equivocada
(`deleteObservation` vs. la columna real `deleteObservacion`) y escribía columnas
sin verificar que existieran en la tabla.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Traits/RegistersUserEvents.php` | Reescrito: escribe `deleteUser_id`/`deleteObservacion` **solo si la columna existe**, ignora el borrado físico y usa `saveQuietly()`. |
| `tests/Feature/DiaExcepcionalControllerTest.php` | El test de borrado verifica que se graba `deleteUser_id`. |

### Solución
El trait ahora rellena las columnas de auditoría con el nombre correcto y
**solo cuando existen** en la tabla del modelo (`Schema::hasColumn`), así no
vuelve a romper si un modelo tiene los campos a medias. En un `forceDelete` no
graba autor (la fila desaparece). El borrado lógico graba `deleteUser_id` (y
`deleteObservacion` si se envía).

### Verificación
```bash
php artisan test --compact --filter="DiaExcepcionalControllerTest"
```

---

## Trabajo 12 — Auditoría (quién registró/eliminó) en todo el sistema

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(auditoria): trait de auditoría en todos los modelos de dominio`

### Problema
El trait de auditoría `RegistersUserEvents` estaba solo en «Días excepcionales».
Se pidió que **todo el sistema** registre quién dio de alta y quién eliminó, y
que **nada se borre físicamente**: siempre eliminación lógica.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Models/{Equipo,Persona,Asistencia,Turno,Profesion,Licencia,AsignacionTurno}.php` | Usan `RegistersUserEvents` (DiaExcepcional ya lo tenía). |
| `app/Traits/RegistersUserEvents.php` | Cachea las columnas por tabla (una sola consulta de esquema por proceso), clave para cargas masivas. |

### Solución
Los **8 modelos de dominio** usan ahora el trait de auditoría: al crear graban
`registerUser_id` (usuario logueado) y al eliminar `deleteUser_id` (+
`deleteObservacion` si se envía). Todos usan **SoftDeletes**, así el borrado es
siempre lógico (marca `deleted_at`, no borra la fila); no hay ningún
`forceDelete`/`truncate` en el sistema. El trait solo escribe las columnas de
auditoría que existen en cada tabla y cachea el listado de columnas por tabla,
para no consultar el esquema fila por fila (la migración masiva de marcaciones
usa `upsert`, que no dispara eventos). Los modelos `User` y `Role` no tienen
columnas de auditoría: el trait las omite sin romper (igual borran lógico).

---

## Trabajo 13 — Filtros de tabla estilo DataTables en todos los listados

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(ui): filtros de tabla (Mostrar N registros + buscador) en todos los listados`

### Problema
Cada listado tenía (o no) su propio buscador, con estilos distintos. Se pidió
unificarlos al estilo «DataTables»: **«Mostrar N registros»** a la izquierda y un
**buscador** a la derecha, arriba de la tabla. Además, mostrar el **ID** de la
fila en Días excepcionales.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/views/components/tabla-filtros.blade.php` | **Nuevo:** componente reutilizable (Mostrar N + buscador, con slot para filtros extra). |
| `resources/views/layouts/app.blade.php` | Estilos `.tabla-filtros` / `.buscador`. |
| `app/Http/Controllers/Controller.php` | Helper `porPagina()` (valida `?por_pagina=` contra 10/25/50/100). |
| `app/Http/Controllers/{Persona,Equipo,User,Role,DiaTurno,Marcacion,DiaExcepcional}Controller.php` | Cada `index` soporta `por_pagina`; Equipos/Usuarios/Roles suman buscador. |
| `resources/views/{funcionarios,equipos,usuarios,roles,horarios,marcaciones,dias-excepcionales}/index.blade.php` | Usan `<x-tabla-filtros>`; Horarios/Marcaciones conservan sus filtros extra (día, fechas, tipo) en el slot con autoenvío. |
| `resources/views/dias-excepcionales/index.blade.php` | Columna **ID** de registro en el listado. |
| `database/migrations/*_create_dia_excepcionals_table.php` | **Bug fix:** se restauró el índice `unique()` de `fecha` (lo necesita el `upsert` de la migración; sin él, `sia:migrar-dias-excepcionales` fallaba). |

### Solución
Se creó un componente Blade `<x-tabla-filtros>` que arma la fila «Mostrar N
registros» + buscador, y se aplicó a los **siete** listados (Funcionarios,
Equipos, Usuarios, Roles, Horarios, Marcaciones y Días excepcionales). El helper
`porPagina()` en el controlador base centraliza el selector de cantidad (10/25/
50/100). Horarios y Marcaciones mantienen sus filtros propios (día / rango de
fechas / tipo) dentro del mismo formulario, con autoenvío al cambiar. De paso se
restauró el índice único de `fecha` en la tabla de días excepcionales, que se
había perdido y rompía la migración masiva.

### Verificación
```bash
php artisan test --compact
```

---

## Trabajo 14 — Las marcaciones del biométrico se guardan en MySQL local

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(asistencias): enviar/importar marcaciones a la base local en vez del SIA`

### Problema
El botón «Enviar a la BD» (sincronizar equipo) y la importación de CSV todavía
escribían las marcaciones en el **SIA** (SQL Server) vía `RegistroAsistenciaSia`.
Se pidió que la información del biométrico vaya a la tabla **`asistencias` de
MySQL** (la base propia del sistema).

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Services/RegistroAsistencia.php` | **Nuevo:** registra marcaciones en `asistencias` local (cruza por `personas.ci`, sin duplicar por ci+fecha+hora, descarta fechas futuras). |
| `app/Http/Controllers/EquipoController.php` | «Enviar a la BD» usa el servicio local. |
| `app/Http/Controllers/MarcacionController.php` | El import de CSV usa el servicio local. |
| `tests/Feature/{Equipo,Marcacion}ControllerTest.php` | Los tests de sincronizar/importar siembran funcionarios locales y verifican `asistencias`. |
| `database/migrations/*_create_turnos_table.php` | **Fix:** faltaba `;` y el índice `unique()` de `idTurno` (rompía todas las migraciones y el upsert de horarios). |
| `app/Console/Commands/MigrarDiasExcepcionalesSia.php` | Idempotente por `updateOrCreate` (no depende de un índice único en `fecha`). |

### Solución
Se creó `App\Services\RegistroAsistencia`, gemelo local del de SIA, que inserta
en `asistencias` (MySQL) cruzando el ID del reloj contra `personas.ci`, sin
duplicar y descartando fechas basura del reloj. Tanto «Enviar a la BD» como el
import de CSV usan ahora este servicio: **lo que se baja del biométrico queda en
la base propia** y aparece de inmediato en el listado de Marcaciones. De paso se
corrigieron dos migraciones que habían quedado rotas (sintaxis y el único de
`turnos`) y se hizo idempotente la copia de días excepcionales sin depender de un
índice único.

### Verificación
```bash
php artisan test --compact --filter="EquipoControllerTest|MarcacionControllerTest"
```

---

## Trabajo 15 — Marcación manual (tipo M) desde la pantalla de Marcaciones

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(marcaciones): alta manual de marcación tipo M`

### Problema
En Marcaciones solo se podía importar/sincronizar del biométrico. Faltaba poder
**registrar una marcación a mano** (tipo M) para un funcionario, sobre la base
local.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Requests/StoreMarcacionRequest.php` | **Nuevo:** valida CI (funcionario existente), fecha y hora; autoriza con `Create:Asistencia`. |
| `app/Http/Controllers/MarcacionController.php` | Método `store`: crea la marcación tipo **M**, sin duplicar (ci+fecha+hora). |
| `routes/web.php` | Ruta `POST marcaciones` (`marcaciones.store`). |
| `resources/views/marcaciones/index.blade.php` | Botón **«Nueva marcación»** + modal (CI, fecha, hora). |
| `tests/Feature/MarcacionControllerTest.php` | 5 pruebas: alta tipo M, validación, CI inexistente, no duplicar, permiso. |

### Solución
Se agregó un botón «Nueva marcación» en Marcaciones que abre un modal con CI,
fecha y hora. Al guardar, se crea una marcación **tipo M** en `asistencias`
(MySQL), cruzando el CI contra los funcionarios y sin permitir duplicar la misma
ci+fecha+hora (respeta el índice único de la tabla). La hora se guarda sobre la
fecha base 1899-12-30, como el resto.

### Verificación
```bash
php artisan test --compact --filter="MarcacionControllerTest"
```

---

## Trabajo 16 — Funcionarios con dos fuentes: «Mamoré» (API externa) y «SIAT» (MySQL)

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(funcionarios): listar desde API Mamoré o base local (SIAT) con un select`

### Problema
Se pidió listar los funcionarios desde **dos fuentes**, elegibles con un select:
**Mamoré** (una API REST externa de solo lectura de Datos Personales, por
defecto) y **SIAT** (la base local MySQL). Con Mamoré la tabla se llena desde la
API; con SIAT, desde MySQL.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Services/MamoreClient.php` | **Nuevo:** cliente HTTP de la API Mamoré (header `X-API-KEY`): `people()` (lista) y `personByCi()` (detalle). |
| `app/Exceptions/MamoreException.php` | **Nueva:** error legible de la API. |
| `config/services.php` + `.env(.example)` | Config `services.mamore` (`MAMORE_API_URL`, `MAMORE_API_KEY`). |
| `app/Http/Controllers/PersonaController.php` | `index` con fuente Mamoré/SIAT (default Mamoré); ficha `mamoreShow` por cédula. |
| `routes/web.php` | Ruta `funcionarios/mamore/{ci}` (antes del resource). |
| `resources/views/funcionarios/index.blade.php` | Select de fuente + tabla normalizada + aviso de error de la API. |
| `resources/views/funcionarios/mamore-show.blade.php` | **Nueva:** ficha de solo lectura de una persona de Mamoré. |
| `tests/Feature/PersonaControllerTest.php` | Tests SIAT (con `fuente=siat`) + tests Mamoré con `Http::fake` (lista, error, sin configurar, ficha, 404). |

### Solución
El listado de Funcionarios tiene un select **Mamoré / SIAT** (por defecto
Mamoré). Con SIAT lee la base local MySQL como antes; con Mamoré consulta la API
externa (paginación y búsqueda incluidas) usando el header `X-API-KEY`, y
normaliza ambas fuentes a la misma tabla. Si la API falla o no está configurada,
se muestra un aviso y la tabla queda vacía (no rompe). El «Ver» abre la ficha:
la local con sus marcaciones, la de Mamoré de solo lectura desde la API. La clave
y la URL viven en el `.env` (`MAMORE_API_URL`, `MAMORE_API_KEY`).

La tabla muestra el **nombre completo** en la primera columna (antes iban paterno/
materno/nombres por separado). La API de Mamoré **busca por un solo término** (no
cruza nombre + apellido): para búsquedas de varias palabras se trae un lote por el
término más largo y se **filtra localmente** por todas las palabras (así «milton
añez» encuentra a «JOSE MILTON AÑEZ PAZ»). La fuente SIAT ya cruzaba nombre y
apellidos con su propio buscador.

### Verificación
```bash
php artisan test --compact --filter="PersonaControllerTest"
```

---

## Trabajo 17 — Marcaciones: nombre del funcionario desde Mamoré (con fallback local)

**Commit:** `(pendiente de commit)` — mensaje sugerido: `feat(marcaciones): resolver el nombre del funcionario por Mamoré y si no por la BD local`

### Problema
En Marcaciones la columna **Funcionario** salía del `ci` cruzado solo contra la
base local. Se pidió que el nombre venga **primero de Mamoré** (por CI) y, si esa
persona no está en Mamoré, **de la base local MySQL**.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/MarcacionController.php` | `index` resuelve los nombres por CI (Mamoré → local) con caché; helpers `resolverNombres`/`nombreMamore`. |
| `app/Services/MamoreClient.php` | Captura errores de conexión y los convierte en `MamoreException` (para poder caer a local sin romper). |
| `resources/views/marcaciones/index.blade.php` | La columna Funcionario usa el nombre resuelto. |
| `tests/Pest.php` | Mamoré desactivada por defecto en tests + `Cache::flush()` por test. |
| `tests/Feature/MarcacionControllerTest.php` | 2 tests: nombre desde Mamoré; fallback a la BD local. |

### Solución
Al listar marcaciones se toman los **CI distintos de la página** y se resuelve el
nombre de cada uno: primero se consulta Mamoré (`personByCi`, **cacheado un día**
por CI para no golpear la API fila por fila) y, si la persona no está ahí (o la
API no está configurada/falla), se usa el nombre de `personas` local. Así el
nombre correcto aparece aunque el funcionario solo exista en uno de los dos
sistemas. Si el CI **no está en ninguno**, la columna muestra «**Sin persona**».
Los fallos transitorios de la API no se cachean (se reintentan) y caen a local
sin romper la pantalla.

### Verificación
```bash
php artisan test --compact --filter="MarcacionControllerTest"
```

---

## Verificación general

```bash
php artisan test --compact
```
```
tests: 175 · passed: 175 · assertions: 517 · duration: 5.43s
```

---

## Informe para presentación (copiar/pegar en Word)

**Informe de trabajo — 24/07/2026**
**Responsable:** nachoDev

**Actividad:** Se trasladó la información del sistema antiguo (SIA) a la base de
datos propia del nuevo sistema, y se empezó a hacer que el sistema use su propia
base en lugar de la antigua.

**Problema detectado:**
El sistema dependía por completo de la base de datos del sistema antiguo para
mostrar funcionarios, marcaciones, horarios y demás. Eso lo ataba a un servidor
viejo que se quiere retirar.

**Solución aplicada:**
1. **Base de datos propia:** se creó en el nuevo sistema una copia de las tablas
   del sistema antiguo (personas, marcaciones, profesiones, horarios, licencias,
   asignación de turnos y días especiales), respetando todos los datos.
2. **Traslado de datos:** se prepararon procesos que copian la información del
   sistema antiguo a la nueva base sin modificar el original, y que se pueden
   volver a correr sin duplicar nada.
3. **Un solo botón de migración:** se dejó un proceso que arma toda la base nueva
   y trae los datos en el orden correcto, listo para ejecutarse cuando se conecte
   al servidor antiguo.
4. **El sistema empieza a usar su base:** las pantallas de Marcaciones, Horarios,
   Funcionarios y Reportes ya leen de la base nueva; quedan por pasar el tablero
   de inicio y la importación de marcaciones.

**Resultado:** El sistema ya tiene su propia base de datos con la información del
sistema antiguo y varias pantallas funcionando sobre ella. Las 155 pruebas
automáticas del sistema quedan en verde. El objetivo final es dejar de usar el
servidor antiguo por completo.

> Nota técnica: el traslado real de los datos se ejecuta desde el equipo Windows
> que tiene conexión al servidor antiguo (SQL Server).

---

## Informe para presentación — segunda parte (copiar/pegar en Word)

**Informe de trabajo — 24/07/2026 (tarde)**
**Responsable:** nachoDev

**Actividad:** Ajustes de acceso y de seguridad de datos, y una pantalla nueva
para administrar los días feriados/excepcionales.

**Trabajo realizado:**
1. **Usuario de acceso:** el sistema queda con un usuario claro para ingresar
   (`admin@admin.com`).
2. **Funcionarios solo consulta:** la pantalla de Funcionarios pasó a ser de solo
   lectura; el alta y la edición se siguen haciendo en el sistema de escritorio.
3. **Nada se borra de verdad:** se dejó que **toda** eliminación del sistema sea
   «lógica» (el dato se marca como eliminado pero no se pierde), incluidos los
   roles de usuario. De paso se corrigió un error que impedía crear la base desde
   cero.
4. **Ordenar la información:** se alinearon los datos que cada tabla puede
   guardar (observación y estado), cuidando que nadie pueda alterar desde un
   formulario quién registró o eliminó un dato.
5. **Días excepcionales:** se agregó al menú un apartado **Parámetros** y dentro
   **Días excepcionales**, donde se pueden registrar, listar, editar y eliminar
   los feriados y fechas que no controlan asistencia, con un **buscador** por
   motivo o por fecha.

**Resultado:** El sistema quedó más seguro (no se pierde información al eliminar),
más claro para ingresar y con una pantalla nueva para administrar los días
excepcionales. Las **161** pruebas automáticas quedan en verde.

---

## Lo que sigue (próximos pasos)

- **Registrar los permisos nuevos en la base real:** correr
  `php artisan db:seed --class=RolesAndPermissionsSeeder` en MySQL para que los 5
  permisos de «Días excepcionales» queden disponibles y se puedan asignar a roles
  que no sean `super_admin` (el `super_admin` ya los tiene por acceso total).
- **Terminar el «flip» a MySQL:** falta pasar el **tablero de inicio**
  (`DashboardController`, aún lee del SIA). El import de CSV y «Enviar a la BD» ya
  escriben en la base local (Trabajo 14); `App\Services\RegistroAsistenciaSia`
  quedó **sin uso** (se puede eliminar).
- **Más pantallas de Parámetros:** sumar bajo el mismo menú otras tablas de
  configuración del escritorio a medida que se necesiten.
- **Auditoría real:** cuando se implemente, completar desde el servidor los campos
  de «quién registró/eliminó» (hoy quedan reservados en la base, fuera del alta).

---

*Documento de sesión — nachoDev — Gobierno Autónomo Departamental del Beni.*
